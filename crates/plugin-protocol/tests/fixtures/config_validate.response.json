{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "valid": false,
    "errors": ["missing socket_path → set it"],
    "warnings": ["the Event Gateway queue has never delivered anything → check the Request URL in the Slack app"]
  }
}
