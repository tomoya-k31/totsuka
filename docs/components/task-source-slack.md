---
type: Component
title: task-source-slack プラグイン
description: 自分宛の Slack メンションをタスク化し本人名義で代理返信する公式 task_source プラグイン（stdio JSON-RPC 単体バイナリ）。設定スキーマ・TokenGuard（auth.test + apps.connections.open）・Web API / Socket Mode クライアント・メンション検知と Task 正規化・プラグイン内 3 段階リポジトリ解決・下書き提示・承認フローに加え、manifest 雛形・CLI レベル E2E・運用ドキュメントまで完備（エピック #102 完了）。
resource: https://github.com/tomoya-k31/totsuka/tree/main/plugins/task-source-slack
tags: [rust, crate, plugin, task-source, slack, socket-mode, token-guard, thread-key, conversation-continuity]
timestamp: 2026-07-25T15:00:00Z
status: active
owner: tomoya-k31
---

# 責務

自分（`target_user_id`）宛の Slack メンションを検知して totsuka のタスクへ正規化し、承認後に **本人名義**（ユーザートークン `xoxp-`、Bot なし）でスレッド返信するための公式プラグイン。[plugin-protocol](/components/plugin-protocol.md) を実装する単体バイナリで、stdio JSON-RPC 2.0（NDJSON）サーバとして起動する。構造は [task-source-notion](/components/task-source-notion.md) / [task-source-github](/components/task-source-github.md) に準拠。コア機能は orchestrator-core / plugin-protocol への変更なしで成立した（エピック #102 の設計判断。その後、設定重複解消のための追加的なプロトコル拡張を任意 issue として採用: #109 で initialize でのリポジトリ一覧供給 — protocol 0.1.1、#119 で initialize での `[llm]` 供給 — protocol 0.1.2）。

現在の実装範囲: crate 構成・設定スキーマ・stdio JSON-RPC ディスパッチ・TokenGuard（#103）、**Slack Web API の型付きクライアント層** と **Socket Mode WebSocket クライアント**（#104）、**メンション検知 → Task 正規化 → `task/submit` push のパイプライン**（#105、#187 で protocol 0.1.6 の push 型へ移行 — [ADR-0008](/decisions/adr-0008-task-submit-push-ingestion.md)。プラグイン内タスクバッファは廃止され、orchestrator が persist してから ack を返すため再起動で ack 済みタスクは失われない）、**プラグイン内リポジトリ解決**（#106）、**下書き提示・承認フロー**（`result/publish` → スレッド内エフェメラル + self-DM 記録 → 承認/却下ボタン → 承認時のみ本人名義スレッド返信、#107）、**運用整備**（Slack アプリの [manifest 雛形](https://github.com/tomoya-k31/totsuka/blob/main/plugins/task-source-slack/manifest.yml)・CLI レベル E2E・[Quickstart Runbook](/operations/slack-quickstart.md)・[トークン取り扱いポリシー](/security/slack-user-token.md)、#108）まで — エピック #102 の全実装 issue が完了。`task/update_status` は意図的な no-op（Slack に動かすステータス列がなく、下書きのライフサイクルはボタンが駆動する）。設計判断の記録は [ADR-0003](/decisions/adr-0003-slack-reply-assistant.md)。

# モジュール構成

| モジュール | 内容 |
|---|---|
| `config` | `plugins/slack.toml`（= `InitializeParams.config`）を型付け。`app_token`（`xapp-`, Socket Mode 用）/ `user_token`（`xoxp-`, 本人名義）/ `target_user_id` / `thread_context_limit`（既定 6）/ `reply_style` / `source_name`（既定 `slack`）/ `[llm]`（リポジトリ選択用 OpenAI 互換 LLM: base_url / model / api_key / confidence_threshold 既定 0.6。**#119 から省略可** — 省略時は orchestrator が initialize で供給する `[llm]` が default になり、明示時はそちらが優先）/ `[[channel_groups]]`（prefix→候補 repos, 定義順 first-match）/ `[[repos]]`（候補リポジトリ: name / summary? / path?。**#109 から省略可** — 省略時は orchestrator が initialize で供給する `[[repositories]]` が候補になり、明示時はそちらが優先）/ `api_url`（テスト用上書き）/ `state_dir`（下書きストアの状態ディレクトリ root 上書き — 既定 `${XDG_STATE_HOME:-~/.local/state}/totsuka`、主にテスト用、#122）/ `max_retries`。`deny_unknown_fields`。静的検証 `static_config_errors`: トークン prefix 形式、`[[repos]]` 重複、channel_groups→repos の参照整合（repos 未確定 = 空のときは initialize まで保留）、confidence_threshold 範囲。「候補 2 件以上での `[llm]` 必須」はオフラインでは供給有無が不明のため #119 で initialize へ繰り延べ |
| `transport` | `SlackTransport` trait（`call(token_kind, method, body, idempotent)` + `post_url`＝`response_url` への POST）＋ reqwest 実装 `ReqwestTransport`。引数は **フォームエンコード** 送信（Slack の read 系 API は JSON body 非対応。`blocks` 等のネスト値は JSON 文字列）。リトライ規律: 通常の一過性エラー（5xx・ネットワーク）は冪等呼び出しのみ指数バックオフ（上限 60s）、**HTTP 429 は `Retry-After` を正確に尊重し非冪等でもリトライ**（429 は拒否済みの保証があるため安全）。コール毎のリトライ総待機バジェット（既定 90s）を超える待機は即時失敗（initialize が無応答に見えないため）。`response_url` POST はリトライなし（発行 30 分・5 回制限）。`expect_ok` で Slack の `{"ok": bool}` エンベロープを解釈。録画レスポンスでテストするための seam |
| `slack_api` | `SlackApi<T: SlackTransport>` — 上位ロジックが Slack と通信する唯一の窓口となる型付きラッパ: `auth.test` / `apps.connections.open`（App トークン）/ `conversations.replies` / `conversations.open`（self-DM 解決）/ `users.info`（display_name→real_name→name フォールバック）/ `chat.getPermalink` / `chat.postMessage`・`chat.postEphemeral`（非冪等＝自動リトライなし）/ `chat.update`（冪等）/ `response_url` POST。共通エラーハンドラが失効系（`invalid_auth` / `token_revoked` / `account_inactive`）をトークン種別に応じたガイダンス付き `Auth` へ昇格し tracing へ出力。`auth.test` のみ **全** API エラーを credential 扱い（引数がなく失敗要因はトークンのみのため） |
| `error` | `SlackError`（Auth / IdentityMismatch / Api / RateLimited / Http / Transport / Timeout / InvalidResponse / InvalidRequest）。`is_retryable`（RateLimited・5xx・ネットワーク）/ `is_rejected`（429＝未適用の保証、非冪等でも再送可）/ `is_credential`（TokenGuard 系）を分類。`auth_failure`（user トークン）と `app_auth_failure`（App-Level トークン＝xapp 再生成手順）が原因別の回復手順付きメッセージへ写像 |
| `socket_mode` | Socket Mode WebSocket クライアント（tokio-tungstenite）。ライフサイクル: `apps.connections.open`（App トークン）→ WSS 接続 → `hello` → envelope 読み取り。**ack 規律**: `events_api` / `interactive` envelope は受信後すぐ ack を送信し（Slack は約 3 秒で再配送するため下流処理を待たせない）、正規化済みイベント `SocketEvent::Message`（message イベントのみ）/ `SocketEvent::BlockActions` を ack 後に **unbounded** チャネルへ配送（有界だと遅い消費者が読み取りループを塞ぎ後続 ack が遅延するため）。`disconnect` / Close / クリーン EOF は `hello` 済みセッションのみ即時再接続（`hello` 前に死んだ接続は失敗としてバックオフ）、`idle_timeout`（既定 60s）無通信はサイレント切断とみなし再接続。接続失敗は上限付き指数バックオフ（transport と共有の `capped_backoff`）、連続失敗閾値超で warn。`apps.connections.open` の API エラーは全て設定不備（missing_scope / not_allowed_token_type 含む）として xapp 再生成ガイダンス付きでループを停止。Receiver drop で待機中でも即終了 |
| `mention` | メンション判定表（この順で first-hit）: ①`subtype`/`bot_id` あり ②送信者が本人 ③self-DM チャンネル ④`<@target_user_id>`（`<@U…|label>` 形式も可）不含 ⑤`channel:ts` 処理済み（上限 1024 の LRU、再起動で消失は許容 — orchestrator 側 ingest も冪等）→ すべて通過で `Mention`（task_id=`{channel}:{ts}`、reply_ts=`thread_ts ?? ts`、**thread_key=`{channel}:{reply_ts}`**＝会話継続キー #140。スレッド内の追いメンションは同一 thread_key を共有し、トップレベルメンションは自身の ts が根 = thread_key == task_id で新規会話） |
| `pipeline` | Socket Mode イベントの常駐消費タスク。self-DM チャンネルを起動時に解決（失敗は警告のみ、判定表②が防波堤）。fresh mention を users.info / conversations.info（ラン内キャッシュ。**成功のみキャッシュ**し、失敗時の生 ID フォールバックはキャッシュしない — 一時障害や `missing_scope` の修正後、再起動なしで次の lookup から回復する、#129）・conversations.replies（直近 `thread_context_limit` 件、話者名付き）・chat.getPermalink で強化し、共通 Task スキーマへ正規化（**instructions=指示文 + reply_style**（protocol 0.1.5。人間可視の body から分離し、ホストが不可視プロンプト注入で帯域外配送できる — フック非対応エージェントへは core が可視 extra_context にフォールバック）、**body=メンション + スレッド文脈のみ** の構造化 Markdown、repo_hint は repo_resolver の解決結果、**thread_key=`{channel}:{reply_ts}` を全タスクに付与** — #140 の[会話継続](/glossary/conversation-continuity.md)キー。プラグイン内に会話インデックスは持たず相関は core の DB に委ねる[再起動を跨ぐため]）。解決は per-mention の spawn タスクで実行（遅い LLM 呼び出しがイベントループ＝ボタン応答を頭詰まりさせない）。強化の失敗は劣化（生 ID・文脈欠落注記・url なし）で吸収しタスクは捨てない。リポジトリ解決が `NeedsSelection` の場合はスレッド内エフェメラル（候補ボタン + スキップ、本人のみ可視）を提示して **Task を提出せず** 保留（orchestrator 側に pending を作らない）。`block_actions` の `select_repo_*` で再開・提出、`skip_mention` で破棄、押下元エフェメラルは `response_url` で書き換え。未回答は TTL 24h（1h 間隔掃除）で破棄（ログのみ、issue #106 の仕様）。保留ストアは上限 256（FIFO 追い出し + warn）。候補ボタンは actions ブロック 25 要素制限に合わせチャンク分割。エフェメラル投稿自体が失敗した場合は hint なしで提出（黙って座礁させない）。タスクは build 直後に SDK の `SubmitClient` で `task/submit` 送信（ack 3 値: accepted はログのみ / duplicate は破棄 / rejected・GaveUp は pending も取り消し[Slack 上で再メンションすれば再試行可]。pending は publish 競合を避けるため送信**前**に登録）。`SharedState` = pending index（task_id → `PendingMention` = channel/reply_ts/送信者 Slack user id（`sender_id`＝`Mention.user` 由来、承認済み返信の `<@…>` メンション前置に使う）/送信者名/permalink。issue の PendingTaskIndex に相当、上限 1024 の FIFO 束縛、`result/publish` が take して消費）+ 下書きストア + 解決済み self-DM チャンネル。`approve_reply` / `reject_reply` の block_actions は `approval` へ委譲（action_id 空間は selection 系と分離） |
| `draft` | 承認フローの下書きストア（#107、#122 で永続化）。`Draft`（task_id / channel / reply_ts / mention_ts / 送信者名 / permalink / 返信文 / dm_ts / status: Pending・Sent・Rejected / created_at: **SystemTime**（プロセス跨ぎ TTL 判定のため epoch ベース））を `draft_id` キーで保持（HashMap、上限 1024 の FIFO 追い出し + warn）し、**mutation 毎（insert / dm_ts / status / sweep）に `drafts.json` へ全量 atomic 書き出し**（`{version: 1, seq, drafts[]}`、KB オーダー）。`initialize` でロードし TTL 超過を prune — **Sent / Rejected も保持**するため二重押下ガード「処理済みです」が再起動を跨ぐ。破損・欠損・未知 version のファイルは warn + 空ストアで安全起動（起動不能にしない）。書き込み失敗も warn のみで in-memory 続行（#122 以前の挙動へ劣化）。draft_id は uuid 依存を足さず「時刻 nanos(16進) + 連番」で生成（seq はファイルから引き継ぎ）。TTL 24h、パイプラインの 1h 掃除 tick で失効 |
| `persist` | ストア永続化の下回り（#122）。XDG 解決（絶対 `XDG_STATE_HOME` → `$HOME/.local/state`、相対値は無視 — core の `paths.rs` と同じ意味論を arch 境界のためプラグイン内に再実装）で `{state_dir}/plugins/{source_name}/drafts.json` を導出（`state_dir` は config 上書き可、解決不能は warn + in-memory 劣化。`source_name` が単一のパスセグメントでない場合[`..`・区切り文字入り等]も同様に拒否 — path traversal 防御）。atomic write は temp ファイル + rename、rename 前に 0600（中身は下書き本文とスレッド座標のみ・トークンなし） |
| `approval` | `result/publish` の下書き提示と承認/却下処理（#107）。publish: PendingMention を take（publish がタスクの終端）→ content から返信文を抽出（**両端のみ** ログ様行 = 空行・ログレベル prefix・ISO 日付 prefix を防御的トリム。中間は温存、全消しになる場合は全文へフォールバック）→ `pending.sender_id` から `<@sender_id> ` を返信文の先頭に機械的に前置（LLM 非経由の決定的コード。送信者への通知を Slack 標準のメンション付き返信と揃える）→ Draft 保存 → Block Kit（検知ヘッダ[送信者 + permalink] / 返信案本文[3000 字制限に合わせ表示クリップ、送信は全文] / 承認[confirm ダイアログ必須]・却下ボタン `value = {"d": draft_id, "c": channel, "ts": reply_ts}`（#121。スレッド座標を埋め込み、Draft 消失後の押下でも投稿先スレッドを特定できる） / context[draft_id・source]）をスレッド内エフェメラル + self-DM 記録（`unfurl_links: false`、返却 ts を dm_ts に保存）の 2 面に投稿。片面の失敗は warn のみで継続、両面失敗は返信文ごと error ログ（黙って喪失しない）。RPC エラーは下書きを作れない場合のみ（pending 不在 = 再起動後・空 content）。block_actions: Draft 不在（再起動・TTL・FIFO 追い出し）→ 「期限切れ」通知（#121: value に座標があれば `chat.postEphemeral` で **元メンションスレッド内** に再実行案内付きで投稿し、押下面が別チャンネル[self-DM]なら `response_url` にも短い案内を追加。座標なし[旧形式の bare draft_id — パース fallback で受理、デプロイ跨ぎの旧ボタン互換]・postEphemeral 失敗時は従来どおり `response_url` へ通知。いずれも `replace_original: false` で元メッセージ温存）、`status != Pending` → 「処理済み」通知（二重送信防止）、承認 → `chat.postMessage`（user トークン = 本人名義、`thread_ts = reply_ts`）成功時のみ `Sent` 化。押下面がスレッド内エフェメラルなら `response_url` の `delete_original` で即削除（`ts` を持たないエフェメラルを消せる唯一の手段。ただし `dm_ts` 無し = self-DM 記録が存在しない場合はエフェメラルが唯一面なので削除せず `replace_original` で ✅/❌ 化し結果を残す）、self-DM 記録なら `response_url` で ✅ に replace（永続面は残す）。押下元が self-DM でなければもう一方の self-DM 記録を `chat.update` で ✅ 化、送信失敗は Pending 維持 + エラー通知（再押下でリトライ可）、却下 → 送信なしで `Rejected` 化しエフェメラルは削除・self-DM 記録は ❌ 更新。イベントループが block_actions を直列処理するため二重押下は競合しない |
| `repo_resolver` | 3 段階リポジトリ解決の ①② 段。① `[[channel_groups]]` prefix ルール（チャンネル名前方一致・定義順 first-match、不一致は全 `[[repos]]` が候補）で絞り込み、候補 1 件なら即確定。② 複数候補は `llm` の分類へ。②の全失敗形態（低確信度・不正応答・API 失敗・`[llm]` 未設定）は `NeedsSelection` に劣化し ③（エフェメラル選択）へ。確定 Task は常に repo_hint 付き = orchestrator の F-10 で即決（コアの LLM repo_select には到達しない） |
| `llm` | プラグイン内蔵の OpenAI 互換分類器。`{base_url}/chat/completions` に mention 本文 + スレッド文脈 + 候補ごとの name / summary / README 先頭 30 行（`path` 設定時のみ・読めなければ無視）を渡し、`{"repo", "confidence", "reason"}` の JSON を強制（コードフェンス・前後プロトコル逸脱は許容パース）。不正応答は 1 回リトライ。confidence < 閾値 / 候補外の repo / API 失敗はすべて `ClassifyError` として ③ へ。`ChatTransport` seam でテスト可能 |
| `server` | JSON-RPC ディスパッチ `Server<F: TransportFactory>`。`Server::new(factory, SubmitClient)`（#187: SDK の stdio ランタイム[単一 writer タスク]で駆動され、`LineHandler` 実装経由で serve される。パイプラインへ `SubmitClient` を配線）。initialize（`[[repos]]` 省略時は供給された `repositories` へフォールバック、`[llm]` 省略時は供給された `llm` を default として採用[#119。base_url / model / api_key が全て非空の usable なもののみ — キーなし供給は「未供給」扱い] → 有効候補の検証[空なら両方の宣言先を示して `CONFIG_INVALID`、候補 2 件以上で `[llm]` がどちらにも無ければ両方の設定先を示して `CONFIG_INVALID`、保留していた参照整合もここで実施] → TokenGuard → Socket Mode + パイプラインの常駐ランタイム起動。`without_runtime()` はプロトコルテスト用）/ config·validate（**静的検証のみ・ネットワーク不要**）/ shutdown / tasks·fetch（**deprecated の空スタブ** — `task_submit` capability 宣言により orchestrator はポーリングしない。0.2.0 で削除予定）/ task·update_status（意図的 no-op）/ result·publish（`approval::publish_draft` へ委譲。pending 不在・空 content のみ `INTERNAL_ERROR`）。未初期化メソッドは拒否。Session drop で常駐タスクを abort |
| `main` | `#[tokio::main]` の stdio ループ。`ReqwestFactory` を配線。ログは stderr |

# TokenGuard（起動時検証）

`initialize` 内で `auth.test`（user_token）→ identity 検証 → `apps.connections.open`（app_token）を実行し、失敗時は原因別ガイダンス付きエラーで初期化を拒否する:

- `invalid_auth` / `token_revoked` / `account_inactive` → トークン再発行・アプリ再インストール・Keychain 更新手順を含むメッセージ（`CONFIG_INVALID`）
- `auth.test` 成功時は `user_id == target_user_id` を検証。不一致は **なりすまし防止** のためエラー（両 ID と修正手順を明示、`CONFIG_INVALID`）
- `apps.connections.open` で App-Level Token（`xapp-`）も同期検証（#108）。これがないと xapp 失効は背景の Socket Mode ループでしか露見せず、`initialize` の呼び手（= `doctor` プローブ）に見えない
- ネットワーク障害などクレデンシャル以外の失敗は `INTERNAL_ERROR` として区別

`config/validate` は意図的にオフライン（静的検証のみ）。ホストのプローブ（`totsuka config validate` / `doctor`）は launch ハンドシェイクで `initialize` も呼ぶため、TokenGuard（両トークンの疎通）はそこで実行される。

# capabilities（F-83）

manifest（`plugins/task-source-slack/plugin.toml`）と `initialize` 応答で `kind = task_source`・`outputs = ["source"]` を宣言（返信は `result/publish` でソース＝Slack スレッドへ書き戻す）。

# テスト

`SlackTransport` を録画レスポンスの fake（`tests/common/`、各テストクレートで共有）に差し替えた 4 系統 + トランスポート実挙動の 1 系統: `tests/slack_api.rs`（全ラッパのリクエスト形状＝メソッド・トークン種別・引数・冪等性クラス、応答パース、失効系の共通ガイダンス、auth.test の全エラー credential 扱い）、`tests/web_api_http.rs`（raw TCP の実 HTTP モック・依存追加なしで、フォームエンコード・bearer 切替・429/Retry-After 尊重・冪等/非冪等リトライ規律・バジェット即時失敗・不正 Retry-After の保守的フォールバックを検証）、`tests/mention_flow.rs`（常駐ランタイム ON: ローカル WebSocket モック + 録画 Web API でメンション → タスク化 → `task/submit` 観測（SubmitHarness）、リポジトリ解決 3 段の分岐）、`tests/approval_flow.rs`（#107 受け入れ: publish での 2 面提示[エフェメラルのスレッド座標・ノイズトリム・confirm 付きボタン・self-DM の unfurl_links: false]、承認 → 本人名義スレッド返信 + 押下エフェメラルの削除（`delete_original`）+ self-DM 記録 ✅ 化 + 自動返信の再検知なし + 再押下の「処理済み」通知、却下 → 送信なし・エフェメラル削除 + self-DM ❌ 化、送信失敗 → Pending 維持 + エラー通知 + 再押下リトライ成功、未知 draft のボタン → 「期限切れ」通知（#121: 旧形式 bare value の response_url fallback[互換ピン留め]・座標入り value の元スレッド内エフェメラル・postEphemeral 失敗時の response_url 劣化・self-DM 押下時の押下面への追加案内）、self-DM 押下時の chat.update 省略、片面投稿失敗の許容、**再起動シナリオ（#122）**: publish → サーバ破棄 → 同一 `state_dir` で再 initialize → 承認押下で本人名義返信 + 永続化済み dm_ts への finalize、さらにもう一度の再起動後の再押下が「処理済み」通知になる[Sent の永続化]）、および JSON-RPC 境界越しの結合テスト（`tests/integration.rs`）: TokenGuard 成功→capabilities 宣言、auth 失敗 3 種の原因別ガイダンス、user_id 不一致拒否、ネットワーク障害の INTERNAL_ERROR 区別、オフライン config/validate（正常系はトランスポート無呼び出しを検証）、未初期化拒否、update_status の no-op と pending 不在 publish のエラー、shutdown・パースエラー・通知の各プロトコル配管。実バイナリでも `plugin install` / `enable` / `config validate`（正常＋未知キー・トークン形式・repos 不整合）/ `doctor` プローブ、およびローカル偽 Slack API と実 Slack API（invalid_auth）双方に対する TokenGuard を確認済み。さらに CLI レベルの E2E（`orchestrator-cli/tests/slack_e2e.rs`、#108）が、実 `totsuka` + 実プラグインバイナリ + モック Slack（raw TCP HTTP + WebSocket）+ mock agent で「メンション envelope → `task/submit` push → dispatch → `result/publish` → 承認ボタン → user トークンでのスレッド返信 + 両面 finalize」の全ループと `doctor` プローブ（TokenGuard 両トークン）を CI で検証する（この E2E が Slack task_id `{channel}:{ts}` の `:` が git ブランチ名として不正という組み合わせバグを検出 → コア `render_branch` のサニタイズで修正）。

# 依存

- `plugin-protocol`（プラグイン境界）、`reqwest`（Web API）、`tokio-tungstenite` + `futures-util`（Socket Mode WebSocket。TLS は reqwest の `default-tls` に合わせ native-tls）、`tokio`、`serde` / `serde_json` / `semver` / `thiserror` / `tracing`。

# 関連

- [plugin-protocol](/components/plugin-protocol.md)
- [task-source-github](/components/task-source-github.md)
- [task-source-notion](/components/task-source-notion.md)
- [会話継続（conversation continuity）](/glossary/conversation-continuity.md)（`thread_key` の供給元。F-105）
- [フックシグナルフロー](/architecture/hook-signal-flow.md)（メンション → dispatch → 完了 → 検収 → 返信の全体像）
- [Spec §4.2 タスクソース / F-01・F-07・F-51・F-59・F-64・F-65・§4.11 F-105](/product/orchestrator-spec.ja.md)
