---
type: Component
title: orchestrator-core クレート
description: totsuka のコア。ヘキサゴナルアーキテクチャの domain（ドメイン・ステートマシン）/ ports（TaskSource・AgentIde・LlmRouter・SecretStore 等の trait）/ adapters（JSON-RPC ブリッジ・SQLite・Keychain）を担う。
resource: https://github.com/tomoya-k31/totsuka/tree/main/crates/orchestrator-core
tags: [rust, crate, core, hexagonal, xdg, platform, config, sqlite, statemachine, logging, plugin, worktree, git, workflow, scheduler, llm, repo-select, recovery, session, run, output, signal, hook]
timestamp: 2026-07-24T12:00:00Z
status: active
owner: tomoya-k31
---

# 責務

totsuka のビジネスロジックの中核。外部 I/O を持たず、ports の trait 境界を介してのみ外界とやり取りする（ヘキサゴナル）。OS 依存機能は `platform` に隔離する（§5.6）。

# モジュール構成

| モジュール | 責務 | 実装タスク |
|---|---|---|
| `domain` | 純粋なドメイン型。`state`（`transition(from,event)` 純関数、11 状態、F-71。#133 で `Verifying`（human 検収待ち、#131 D-01）/ `Escalated`（人間対応待ち、D-02/D-03。どちらも非 terminal）とイベント `SelfReportComplete`/`ApproveVerification`/`VerificationFailed`/`Escalate` を追加 — Escalate は全非 terminal から到達、Escalated からは次シグナルで Verifying/Publishing/WaitingInput/Running へ復帰）、`signal`（#133。フックシグナルの正規化型 `AgentSignal`/`SignalSource`/`SignalEvent`（Stop/Notification/SessionStart/SessionEnd/Heartbeat）/`StopStatus`/`JobId`（`"job-{task_id}-{session_row}"`、FromStr/Display、不正形式は `InvalidJobId`）。**#178: マーカー提示形の共有定数 `MARKER_COMPLETED`/`MARKER_NEEDS_INPUT`/`MARKER_FAILED`** — 描画側（`hooks` のマーカー規約文）と受信側（`run::hooks` の `MARKER_SELF_REPORT_INSTRUCTION`）の両 instruction をここから組み立て、ワイヤ規約の Rust 側定義を一元化。UDS 受信サーバ＝`adapters::hook_uds`（#136 実装済み）、Engine 配線＝`run::hooks::Engine::on_signal`（#138 実装済み。`hook_uds::parse_signal` はスプール回収でも再利用））、`workflow`（`source×trigger×mode×agent×output` 定義の解釈・トリガーマッチング（定義順 first-match F-81・status/label 防御的再判定）・検証（plan×pull_request エラー F-82、output=source の capability 検証 F-83、trigger 重複警告 F-81）・on_success/on_failure=OutcomeAction F-84） | #48 / #54 / #133 |
| `ports` | 差し替え対象の trait 境界（`SecretStore` / `ProcessProbe` / `LlmRouter` / `GitRunner` / `AgentSession`（再起動時のセッション再接続 F-37）、`SignalPort`（#136。driving adapter がフックシグナルを投入する境界。`submit(AgentSignal) -> Future<SignalAck, SignalError>` の RPITIT、Engine 非依存で hook_uds を隔離）、`FocusPort`/`FocusOutcome`（#155 F-94。`POST /focus` 制御要求の境界。シグナルと違い request-response で、`focused: bool` + 縮退理由を返す — 「フォーカスできない」は正常応答でエラーにしない）、後続で `TaskSource` / `AgentIde`） | 各機能タスク / #136 / #155 |
| `paths` | XDG Base Directory 準拠のパス解決（`config`/`data`/`state`/`cache`/`runtime`、`totsuka` サフィックス、未設定時 `$HOME` フォールバック）。macOS でも XDG を明示採用 | #46 |
| `platform` | OS 依存実装の隔離。`platform::macos`（Keychain = keyring クレート）、`platform::unix`（`ProcessProbe` = `kill(pid,0)`）、`platform::fallback`（非 macOS の未サポート SecretStore）、**`platform::onepassword`（#156、[ADR-0006](/decisions/adr-0006-onepassword-secret-backend.md)。`op read --no-newline` シェルアウトで `op://<vault>/<item>/<field>` を解決。`#[cfg]` ゲートなし = 全ターゲットで動き、非 macOS 初の実働バックエンド。stdout（平文）はログ・エラーに出さず stderr のみで分類 — 未導入 `BackendUnavailable`（brew install 1password-cli）/ item 不在 `NotFound` / 未サインイン「`op signin` を実行」。runner seam で実 `op` 非依存テスト）**。`PlatformSecretStore` は **合成ストア**: `SecretRef` の variant（enum 化 #156）で `Keychain` → Keychain/フォールバック、`OnePassword` → `OnePasswordCli` へ振り分ける | #46 / #156 |
| `config` | 設定ロードと検証。`schema`（`config.toml` パース、F-60/61/64）、`raw`（`plugins/{name}.toml` を無解釈保持 → JSON、F-64）、`resolve`（`${ENV}`/`keychain:` シークレット解決・`~`/`${ENV}` パス展開、F-62/65。**#217 で CLI から移動**: `secret_resolver(env)` = `PlatformSecretStore` + env スナップショットの合成コンストラクタ、`resolve_strings` = 任意 JSON の文字列リーフへの再帰シークレット解決 — store ジェネリックでテストはフェイク注入可）、`env_overrides`（**#208、[ADR-0009](/decisions/adr-0009-env-override-whitelist.md)**: F-66 第 2 層 `TOTSUKA_*` の適用。`apply_env_overrides(&mut RootConfig, env)` が**明示ホワイトリスト**（env 名 → 型付きフィールドの静的表、14 キー）に沿って `config.toml` パース直後の値を上書きし、警告リスト（未知キー・空値）を返す。不正値と「`[llm]` 不在での `TOTSUKA_LLM_*`」は `ConfigError::EnvOverride` で起動エラー、注入系の予約名（`TOTSUKA_JOB_ID` / `TOTSUKA_HOOK_*` / `TOTSUKA_PROMPT_CONTEXT`）は警告なしで無視。優先順位はマージ器ではなく**適用順**で実現する（CLI フラグは呼び出し側でこの後に適用）。旧 `layered`（4 層のフラット文字列 `HashMap` + `ConfigResolver`）は型強制もネストキー規約も持たず `RootConfig` へ接続できないまま**どこからも構築されていなかった**ため削除）、`validate`（静的検証 F-63/58 ＋ ワークフロー検証 F-81/82/83 を統合する `validate()` エントリ、Error/Warning の Finding を返す）、`edit`（`toml_edit` で `[plugins.{name}] enabled` のみ書き換え・コメント整形保持、F-57） | #47 / #52 / #54 |
| `plugins` | プラグインの on-disk ストア（`store`）。install（prepare→confirm→commit の2段、SHA-256 表示 §5.4、manifest/protocol 互換検証 F-54）・uninstall・list。install=バイナリの存在、enabled=設定の宣言を分離（F-56）。**`spec`（#217 で CLI から移動）**: enabled プラグイン 1 つの起動スペック組み立て `plugin_spec(store, plugin_config_dir, cfg, name, env) -> PluginSpec`（F-58/64/65）— store の manifest/バイナリパス + `plugin_init_config`（`plugins/{name}.toml` を `PluginRawConfig` で読み JSON 化 → `resolve_strings` でシークレット解決。欠落は空オブジェクト）+ task_source 向け config.toml 素材（`repo_infos` = `[[repositories]]`→`RepoInfo`（パス展開 best effort）#109、`llm_info` = `[llm]`→`LlmInfo`（`api_key_ref` 解決、解決不能は None の best effort — doctor の `llm` チェックと run 自身のルータ構築が問題を表面化する）#119、workflow trigger + `poll_interval_secs` 0.1.6）を合成。エラーは `SpecError`（未インストール/store/IO/parse/シークレット解決失敗、「原因 + 次のアクション」規約）。`run` / `doctor` / `config validate` の 3 コマンドが共用（doctor と validate は解決済み `init_config` を再利用して Keychain/Touch-ID の二重プロンプトを回避） | #52 / #217 |
| `repo_select` | リポジトリ自動選択（F-10〜15）。repo_hint 解決を最優先、未解決なら LLM 分類（候補=概要＋README先頭N行）。confidence 低/未知repo（1回リトライ後）は pending、API 恒久失敗は failed。`reason` は --dry-run 用。`ReadmeCache`（README hash キャッシュ、F-15）。LLM は `ports::LlmRouter`（OpenAI 互換）越し、reqwest 実装は `adapters::llm::OpenAiRouter`（指数バックオフ §5.3） | #56 |
| `scheduler` | 並列実行制御（F-40〜45）。3 階層スロット（global/repo/agent）を全て満たすと dispatch 可、`counts_toward_slot` は dispatched/running/verifying/publishing を計上（waiting_input/escalated は解放。verifying は出力未確定のため Publishing 相当で保持、escalated は人間待ちのため WaitingInput 相当で解放 #133）。DB から `rebuild` で再構築。`PriorityQueue`（priority 降順→FIFO）、`plan_dispatch`（優先度順にスロット確保） | #55 / #133 |
| `worktree` | git worktree ライフサイクル（F-20〜25/85）。作成（fetch→origin/{default} を commit 解決して分岐→worktree add、並列安全）、掃除（immediate/retention/manual、未コミットは skip。**#210（[ADR-0010](/decisions/adr-0010-worktree-cleanup-pane-release.md)）で判定と実行を分離**: `decide_cleanup`（dirty チェック + policy 判定 → `CleanupDecision::{Remove,Retain,Dirty}`）と `remove`（削除の実行。冒頭で dirty を**再チェック**し、判定〜実行の間 — pane close の窓 — に dirty 化していれば `DirtySkipped` で中止 = TOCTOU 対策。データ損失 > pane 喪失）。既存 `cleanup()` は両者を呼ぶ薄いラッパとして互換維持）、孤児検出。ブランチ/配置テンプレート描画とサニタイズ — `render_branch` は task_id 由来の git ref 禁止文字（`:` 空白 `~^?*[\` 制御文字）を `-` へ合法化する（task_id はソース定義で、Slack は `{channel}:{ts}`。git 制約は git 境界で一括吸収、#108） | #53 |
| `logging` | 構造化ログと機密マスキング。`redact`（フィールド denylist＋値パターン）、`layer`（redact 済み JSON Lines / 人間可読を出力する tracing レイヤ）、`rotation`（日次ログの世代保持）。規約は [ログ規約](/development/logging-conventions.md) | #49 |
| `recovery` | 再起動回復（F-37/44、§5.3）。`recover` が回復対象状態（`RECOVERABLE_STATES` = dispatched/running/waiting_input/verifying/escalated/publishing）のタスクを `session/attach` で再接続し、エージェント状態にステートマシンを前方同期（`resume_plan`）／attach 失敗は「継続確認待ち」（`RecoveryReport`、**自動 failed にしない**）。**human-gated 安全化（#133）**: `verifying`/`escalated` はエージェント状態に関わらず常に Confirm（検収・エスカレーション解消を再起動で自動スキップしない）、`(agent Done, waiting_input)` も自動 Publishing せず Confirm（human 検収相当の検収スキップ穴を封鎖）。`retry_plan`（worktree＋セッション再利用 F-44）・`active_slot_claims`（`SlotManager::rebuild` 用 (repo,plugin)）。詳細 → [state.db スキーマ](/data/state-db.md) | #57 / #133 |
| `tool` | AI ツール抽象（#196、[ADR-0014](/decisions/adr-0014-tool-abstraction.md)）。`ToolKind`（claude / codex / opencode。`has_adapter()` = 完了検知アダプタの有無 — claude / codex / opencode すべて ✓（#196 Phase 1〜3 完了。アダプタ無し kind の拒否経路は将来の kind 追加に備えて残置））、`ToolCapabilities`（不可視注入・marker block・prompt 検証・resume・plan・heartbeat・session id 捕捉 — 縮退制御の静的表。codex は 2026-07-24 実機スパイク（codex-cli 0.145.0）で確定: prompt 検証 ✗・heartbeat ✗ 以外 ✓。opencode は同日スパイク（1.14.39）で確定: 不可視注入 ✗・marker block ✗・prompt 検証 ✗・heartbeat ✗、resume/plan/session id 捕捉 ✓）、`ToolProfile`（`[tools.<name>]` の解決結果。組み込み `claude` / `codex` / `opencode` + `registry_from_config` で組み込みを `[tools]` エントリが上書きするレジストリを構成）、`launch_spec(LaunchInputs) -> Option<ToolLaunchSpec>`（kind ごとの argv 組立。claude: base → plan なら `--permission-mode plan`（上書き可） → `--settings <path>` → `--resume <id>` — 旧 herdr `launch_command` の移設で golden テストも移植、挙動不変。codex（Phase 2）: base → resume なら `resume <id>` **サブコマンド** → implement 既定 `--sandbox workspace-write --ask-for-approval on-request` / plan 既定 `--sandbox read-only`（plan permission mode 不在の縮退、実機スパイク [V3]）。`settings_path` は不使用 — フックはグローバル登録 + env ゲート（`hooks::codex`）。opencode（Phase 3）: base → plan なら `--agent totsuka-plan`（全 deny の plan エージェント） → resume なら `-s <id>`。`settings_path` 不使用（完了検知はグローバル配置の JS プラグイン、`hooks::opencode`））。ツール解決の優先順位は `resolve_tool_name`: workflow ピン > repo 既定 > `default_tool` > 組み込み `claude` | #196 |
| `hooks` | **フックスクリプト + workflow 別 settings のレンダリング（#131 H-01/H-03, #137。#178 で [orchestrator-cli](/components/orchestrator-cli.md) から移動）** — 描画側（本モジュール）と受信側（`run::hooks`）・マーカー定数（`domain::signal`）がこれで単一クレート内に閉じ、フック仕様の変更が 2 クレートにまたがらない。静的スクリプト 6 本（`hook-common.sh` / `on-stop.sh` / `on-notification.sh` / `on-session-start.sh` / `on-session-end.sh` / `on-user-prompt-submit.sh`）を `include_str!` でバイナリ同梱し、`install` が `run` / `doctor` 起動時に `$XDG_DATA_HOME/totsuka/hooks/` へ **0700・内容ハッシュ（SHA-256）冪等**で書き出す（一致なら書き換えず mtime も保存、バージョンアップ時のみリフレッシュ。モードのみのドリフトは内容一致でも再適用して修復）。あわせて **workflow 別 `orchestrator-<workflow>.json`** を **0600** でレンダリング（`settings_path` がパスの単一定義）: `Stop` には常に `on-stop.sh`（command 型）、`verification = "llm"` の workflow のみ `prompt` 型フック（`workflow.rubric` か既定 rubric + 中間停止免除文 + マーカー規約）を追加（D-01）。マーカー規約文は `domain::signal` の共有定数 `MARKER_COMPLETED` / `MARKER_NEEDS_INPUT` / `MARKER_FAILED` から組み立て、受信側 `run::hooks` の `MARKER_SELF_REPORT_INSTRUCTION` も同じ定数から組むためワイヤ規約の Rust 側定義は一箇所（bash 側 `on-stop.sh` のパースは言語跨ぎのため手動同期 + 受信側の same-shape テストで担保）。免除文は `on-stop.sh` の R-02（`background_tasks` 非空 = ハートビート）と対で、バックグラウンドタスク実行中の中間 Stop を判定 LLM が検証・ブロックしないよう機械的に付加する。`Notification` / `SessionStart` / `SessionEnd` / `UserPromptSubmit` は各 command 型フック（`UserPromptSubmit` は全 workflow 共通配線 — スクリプトが env 未設定時に no-op するため常時登録して安全）。**ジョブ固有値はファイルに書かない**: job_id / エンドポイント / トークン / スプールディレクトリ / プロンプトコンテキストは env（`TOTSUKA_JOB_ID` / `TOTSUKA_HOOK_ENDPOINT` / `TOTSUKA_HOOK_TOKEN` / `TOTSUKA_HOOK_SPOOL_DIR` / `TOTSUKA_PROMPT_CONTEXT`）で [agent-ide-herdr](/components/agent-ide-herdr.md) が注入（`HookLaunchSpec`）し、同一の `--settings` パスが `claude --resume` を跨いで再利用できる（H-03）。`verify_assets` は**書き込みなし**の検査（存在・0700/0600・内容ハッシュ一致 → `AssetIssue` の列挙）で、doctor の N-02 タンパー検出に使う。**#196 Phase 2: スクリプトのツール非依存化 + `hooks::codex`（codex 登録管理）** — codex はフックを `$CODEX_HOME/hooks.json` へ**グローバル登録**するため（`--settings` 相当なし）、全スクリプトに `TOTSUKA_JOB_ID` 早期ゲート（未設定 = 個人セッション → 即 exit 0。特に `on-stop.sh` が個人セッションを block しない保証）を追加し、`on-stop.sh` は `prompt_id // turn_id`（codex のターンキー）フォールバック、`on-notification.sh` は codex `PermissionRequest`（Notification イベント不在の代替）を `message = "permission_prompt: <tool_name>"` へ合成して同一ワイヤ形で送出（stdout は常に空 = 承認判断へ不干渉）。`hooks::codex` は totsuka 管理エントリ（Stop/SessionStart/SessionEnd/UserPromptSubmit/PermissionRequest、SessionEnd は codex の 3s クランプに合わせ timeout 3）をコマンドパス基準の構造判定で同期: 非管理エントリは**既存 index のまま**意味的に保全（codex の trust レコードは index キーのため in-place 置換）、パース不能 JSON は上書き拒否、`[hooks.state]` の trust 存在プローブ `untrusted_events` を doctor へ提供（未 trust エントリは codex が**サイレントスキップ**するため警告必須）。同期は config が codex-kind ツールを参照する場合のみ（claude-only 構成は `$CODEX_HOME` に触れない）。**`hooks::opencode`（#196 Phase 3）**: 完了検知 JS プラグイン `totsuka-opencode.js`（session.status(idle)/session.idle 両対応 → `client.session.messages` で最終 assistant メッセージ取得 → マーカー解析（on-stop.sh と同じ 1〜2 カッコ許容・最後勝ち）→ Bun fetch `unix:` で UDS POST。prompt_id = メッセージ id（不在時は任意ロール最終 id へフォールバック — 空文字だと同 status の Stop が冪等キーに畳まれるため）。session.created → SessionStart、session.error → FAILED（per-occurrence id）。POST 失敗は spool。env（TOTSUKA_HOOK_ENDPOINT/JOB_ID）無しではフック未登録 = 個人セッション保護）と plan エージェント `totsuka-plan.md`（permission edit/bash/task **全 deny** — 部分 deny はサブエージェント委譲で貫通する実機発見）を `$XDG_CONFIG_HOME/opencode/{plugins,agents}/` へ SHA 冪等配置。trust 機構は無く配置のみで有効（参照ゲートは codex と共通の `config_references_kind`）。`on-stop.sh` は `set -uo pipefail`（`-e` は使わない = fail-open D-09）: ①`jq`/`curl` 欠落 → 生 JSON をスプールへ追記し exit 0 ②`background_tasks` 非空 → `heartbeat` を POST し exit 0（中間 Stop、R-02）③`last_assistant_message` の**最後の** `<<STATUS:...>>` マーカー（`reason="..."` 属性込み、D-12。**山カッコは単一/二重どちらも許容**）を抽出し該当 status を POST ④マーカー欠落 & `stop_hook_active != true` → `UNKNOWN` を POST の上、stdout に `{"decision":"block",...}`（是正方法つき、R-03）⑤マーカー欠落 & `stop_hook_active == true` → `UNKNOWN` の POST のみ（再 block しない）。POST は `hook-common.sh` の `post_event`（`curl --unix-socket` + Bearer + `--max-time 5 --retry 2`）、失敗時は NDJSON 1 行を `$TOTSUKA_HOOK_SPOOL_DIR/<epoch>-<pid>.jsonl` へ追記（E-07/H-11）。stdout は block JSON 以外を出さない（H-13）。`on-user-prompt-submit.sh` は**不可視プロンプトコンテキスト注入**（`UserPromptSubmit` フック）: env `TOTSUKA_PROMPT_CONTEXT` が設定されていれば `additionalContext` を 1 行だけ stdout へ出力し、env 未設定・空・`jq` 欠落時は無出力で exit 0（fail-open D-09、`on-stop.sh` の block がセーフティネット） | #137 / #178 |
| `run` | `run` メインループ（§5.1）。`Engine` が push（`task/submit`、防御的再マッチ含む）取り込み（F-73）→リポジトリ選択→スロット確保→worktree 作成→`task/dispatch`→`state/subscribe` の起動時サイクルと、`state/notification` 駆動の監視（waiting_input=スロット解放＋Notifier 配送 F-35/45、done=出力ポリシー実行→`on_success`/`on_failure` 書き戻し F-84→worktree 掃除 F-23/85、retry は worktree＋セッション再利用 F-44、プラグインクラッシュ=failed §5.3）を統合するイベント駆動ループ。ワンショット（既定: 全 dispatch 済みタスクが終端/待機に達し、かつ直近イベントから静穏期間（`ONE_SHOT_GRACE` 2 秒）が経過したら summary で終了 — **0.2.0（#190）**: 全 source が push 専用になったため、起動直後にまだ届いていない submit を待たず即終了する潜在バグが露呈し、この猶予窓で修正）／`--watch`（Orchestrator 側のポーリングはなく、push をイベントとして受け続け shutdown future で graceful 停止）／`dry_run`（**0.2.0**: push source はプレビュー対象を持たないため常に空を返す no-op、副作用ゼロ）。`settings_from_config` が `RootConfig` を解釈（`max_concurrency` F-40、`poll_interval_secs`（push source の内部 fetch 周期として `initialize` へ転送、F-06）、`[worktree] cleanup`/`plan_cleanup` F-23/85 — implement 既定 manual・plan 既定 immediate、`[output]` PR テンプレート）。`[llm]` 未設定時は LLM が必要な選択を pending へフォールバック（F-14）。`run::output` サブモジュール（#65）が出力ポリシーを実装: `pull_request`=コミット存在検証（`WorktreeManager::has_commits_to_publish`、ゼロなら failed）→Orchestrator が push（`push_branch`、エージェントは push しない F-86）→PR 作成（`PrCreator` seam、実装 `GhPrCreator`=`gh pr create`、テンプレート `{title}`/`{url}`/`{summary}` 等）、`source`=蓄積したエージェント出力を `result/publish` へ（F-07）、`none`=素通し。plan×pull_request は publish 点でも防御的に拒否（F-82）。publish 失敗は worktree・コミット・セッションを保持したまま failed とし `task retry` で再開可能。#136/#138 で `Engine::run` 開始時に `EngineSettings.hook`（`HookRuntime`＝socket_path＋解決済み Bearer token＋spool_dir＋ワークフロー別 `--settings` パス＋block_retry_limit、CLI が組み立て）があれば `hook_uds::bind`→`serve` を tokio::spawn（`[hooks]` 未設定でも既定パスで常時起動、bind 失敗は warn して継続）、ループ終了時に watch で停止・await。**#138: フックシグナル統合（`run::hooks`）**。`PluginEvent::HookSignal(AgentSignal)` は `on_event`→`Engine::on_signal` へ届き、job_id→task_id 解決（E-09。未知 task は warn ログのみで `hook_events` へ永続化しない＝`task_id` NOT NULL FK のため意図的・状態も一切変えない）→`touch_last_signal`（生存更新は冪等判定より前。重複 heartbeat でもアンカーを更新しタイムアウト誤判定を防ぐ）→冪等記録（`record_hook_event`、`Duplicate` は即 return D-05）→イベント別処理: `Stop{COMPLETED}` は `verification=llm/none` なら既存 Done 経路（マーカー除去済み `last_assistant_message` を `agent_output` へ→`finalize_success`／出力ポリシー無改修 R-07/R-11）・`human` なら `SelfReportComplete`→`Verifying`＋`VerificationPending` 通知＋成果物を永続化（再起動後の検収に備える D-01）、`NEEDS_INPUT`→`WaitingInput`（D-07）、`FAILED`→`Fail`（reason 保存）、`UNKNOWN`→遷移なし・連続 UNKNOWN が `block_retry_limit`（既定 3）以上で `Escalate`（D-02）、`Notification`→通知のみ（承認待ちと質問待ちの区別 R-08）、`SessionStart`→`set_tool_session_id`（相関確立、不一致は警告のみ・Escalate しない E-09）、`SessionEnd`→記録＋警告（Fail にはしない・タイムアウト/デッドマンに委譲 D-10）、`Heartbeat`→`touch_last_signal` のみ。`Escalate` 時は capability `diagnostics_snapshot` があれば `diagnostics/snapshot` RPC で pane スナップショットを取得し `events.detail` へ記録（R-10）＋`Escalated` 通知。`sweep_signal_timeouts()`（cycle ごと）がフック dispatch 済みの非待機タスクで `now - last_signal_at > workflow.timeout_secs`（既定 1800）を `Escalate`（D-03）。`replay_spool()`（recover 直後＋cycle ごと）が `[hooks].spool_dir` の `*.jsonl` を `hook_uds::parse_signal` で正規化し `on_signal` へ再投入（冪等キーで重複無害 E-07）。全行クリーンに処理できたファイルのみ削除し、parse 不能行を含むファイルは `<name>.corrupt` へ隔離リネームして保全（部分書き込み/壊れた1行で全体を失わない）。dispatch 側（`dispatch_one`）はフック対応 agent（判定は [plugin-protocol](/components/plugin-protocol.md) の `Capabilities::hook_capable()` = `resume_session \|\| diagnostics_snapshot`。**#209 でこの OR 判定を単一ソース化**し、CLI 側の `[hooks].auth_token_ref` 検査と同一基準を共有する）に `job_id = job-{task_id}-{session_row}`＋`HookLaunchSpec { settings_path, env: TOTSUKA_JOB_ID/HOOK_ENDPOINT/HOOK_TOKEN/HOOK_SPOOL_DIR/PROMPT_CONTEXT }` を付与（session_row は起動前に `reserve_session` で確保し `task/dispatch` 後 `set_session_native_id` で埋める。dispatch 失敗時は `delete_session` で予約行をロールバックし空 id 行を残さない）。**#196: ツール解決と `tool_launch`** — repo/agent 検査直後に `wf.tool > repo.tool > default_tool` でツールを解決し（未知名・アダプタ無し kind は fail_dispatch）、`ToolProfile::launch_spec` が組み立てた完全解決済み argv/env を `TaskDispatchParams.tool_launch` として送出（deprecated の `hook` も移行窓の間は併送）。thread 継続 resume は `caps.resume && caps.session_id_capture` でゲート。**#196 Phase 3: ケイパビリティ駆動のコンテキスト経路** — `caps.invisible_injection` が無いツール（opencode）へのフック付き dispatch は、`TOTSUKA_PROMPT_CONTEXT` env の代わりに同内容（instructions + マーカー規約）を**可視の extra_context** へ載せる（完了契約は初回ターンから届く。pane に表示される点が claude/codex と異なる）。あわせて**ディスパッチのコンテキスト経路制御**: フック付き dispatch は `Task.instructions`（protocol 0.1.5、task_source 所有の指示文）とマーカー自己申告指示（`MARKER_SELF_REPORT_INSTRUCTION`）を連結して **env `TOTSUKA_PROMPT_CONTEXT`** に載せ、CLI の `on-user-prompt-submit.sh`（`UserPromptSubmit` フック）が `additionalContext` として**不可視注入**する — ペインには本文だけが表示され、初回の指示にマーカー規約が含まれるため**最初の Stop からマーカーが載り、`on-stop.sh` の block による全文再生成（ペインの二重出力）が起きない**（実機検収での発見）。指示には**配信契約**も明記する（`slack-reply` 実機の 2 回目の発見）: ①公開されるのは**マーカー付き最終メッセージのみ**（`on_stop_completed` がその 1 通を成果物として stash する R-07/R-11）なので、最終メッセージは過去メッセージ参照を含まない自己完結の全文であること ②バックグラウンドタスク/サブエージェント実行中はマーカーを出さないこと（その Stop は R-02 ハートビートであり、完了後の再起動ターンで全文＋マーカーを再掲する）。この契約が無いと、回答済みのままバックグラウンド完了で再起動されたターンが「前のメッセージで提示済み」というメタ発言＋マーカーを出し、そのメタ発言が Slack へ公開されていた。このとき `extra_context` は `None`（可視前文は廃止）。マーカー知識は core に閉じ source プラグインは触れない（H-01）。マーカー行は publish 時に除去済み（R-07/R-11）で、`on-stop.sh` の block は付け忘れ時のセーフティネットとして残る。**フック非対応 agent（orca/mock）へのフォールバック**: 不可視チャネルがないため `Task.instructions` を可視の string `extra_context` として渡す（マーカー指示は付けない — フック完了報告を使わないため）。**#140: [会話継続](/glossary/conversation-continuity.md)**。`dispatch_one` はリトライ再利用ブロックの直前で resume 判定（`thread_resume_session_id`）: タスクが `thread_key` を持ち、同一 workflow・同一 thread_key の**先行**タスク（`find_by_thread_key(workflow, thread_key, exclude=自 id)` = 自分以外の最新、E-09）の最新セッションに空でない `tool_session_id` が確立し（フック SessionStart 由来 #138）、agent が `resume_session` capability を宣言していれば、`task/dispatch(resume_session_id)` に先行セッション ID を渡し `claude --resume` で文脈を継続する。worktree は通常フローで新規作成（破棄済みなら再作成 = セッションだけ使い回す D-10）。1 つでも欠ければ警告なしで通常の新規 dispatch へフォールバック（ベストエフォート）。新セッション行の `tool_session_id` に resume 依頼値を仮置きし SessionStart で実値と突き合わせる。先行タスクの行・状態は変更しない。`totsuka task verify --pass`→`ApproveVerification`（Verifying 以外はエラー）→次 recover で `finalize_success`、`--fail --reason`→`VerificationFailed`→Running（D-07）。**#155 (F-94): click-to-focus 制御**。`PluginEvent::Focus { task_id, respond }` を `on_event` が `run::hooks::Engine::focus_task` へ配路: task→`latest_session`→agent プラグイン解決、**`pane_control` 宣言時のみ** `session/focus` RPC（session_id は不透明のまま委譲 F-37）。task 不明/未 dispatch/プラグイン停止/`pane_control` 非宣言/pane 消失はすべて **reason 付き `FocusOutcome`（focused: false）の正常応答**でエラーにしない（[ADR-0005](/decisions/adr-0005-click-to-focus.md)）。**#185 (0.1.6, [ADR-0008](/decisions/adr-0008-task-submit-push-ingestion.md)): push 取り込み（`task/submit`）**。source プラグインごとの forwarder が `IncomingRequest` を消費し、parse（不正は `INVALID_PARAMS`）→ per-plugin Semaphore（枠 64、枯渇は `SUBMIT_OVERLOADED`）→ `PluginEvent::TaskSubmit { source, task, respond: oneshot }` を送出（channel closed → `NOT_ACCEPTING`、`task/submit` 以外は `METHOD_NOT_FOUND`）。ack 待ちだけ spawn し、イベント送出は inline のため per-source FIFO は維持。`Engine::on_task_submit` は防御的 `match_workflow`（不一致 → `Rejected` + reason）の上で `ingest_task()` により **`upsert_task` コミット後にのみ ack**（persist-before-ack、重複は `Duplicate`）。応答後に `select_repos()` → ループ既存の `dispatch_ready()` で通常配車。永続化失敗は plugin へ retryable な `INTERNAL_ERROR` を返した上で run-fatal。`RunStats.submitted` 追加。再起動リプレイは既存機構のまま（persist 済み未 dispatch の `Queued` 行を起動時 cycle が回収、ack≠dispatch）。**#190 (0.2.0, ADR-0008 Phase C): `tasks/fetch` 削除**。`TasksFetchParams`/`TasksFetchResult` ごと削除し、全 task_source が push 専用に。Orchestrator 側のポーリング機構一式（`fetch_and_ingest`/`poll_sources`/`poll_interval_for`/`IngestPath` enum/`EngineSettings.poll_intervals`）を削除、`ingest_task()` は無条件で `upsert_submitted_task` を呼ぶだけに単純化。`RunStats.fetched`/`.ingested` を削除（`fetched` は恒久的に 0・`ingested` は `submitted` と完全重複だったため）。この変更で「one-shot は起動直後の未着 submit を待たない」という潜在バグが露呈したため、`run()` に静穏期間の猶予窓（`ONE_SHOT_GRACE`）を追加して修正した（上記参照）。**さらに**（code review で発見）、旧ポーリング駆動の `cycle()` 呼び出しは実質的に周期メンテナンスの心拍も兼ねていたため、これを削除しただけでは `--watch` の長時間稼働プロセスが `sweep_signal_timeouts()`（D-03）・`sweep_finished_worktrees()`（F-23）を push イベント到着時以外に一切再実行しなくなる regression が生じていた。`SETTLE_TICK`（200ms）の安全 tick を one-shot 専用ガード（`if !watch`）から外し両モード共通の周期 `cycle()` 呼び出しへ変更して修正（`crates/orchestrator-core/tests/hook_integration.rs::watch_mode_periodic_tick_escalates_silent_task_without_events` で固定）。`^0.1` manifest は F-54 により起動拒否（意図された動作）。**#210 (0.2.1, [ADR-0010](/decisions/adr-0010-worktree-cleanup-pane-release.md)): worktree 掃除の3段化と pane 解放**。`cleanup_worktree` は `decide_cleanup`（判定）→ **`Remove` のときだけ** `release_pane`（`session/release` RPC。`pane_control` 宣言 agent のみ・`expect_cwd` = worktree パスを同一性ガードとして送出・`expect_label` は未送出）→ `remove`（削除、dirty 再チェック付き）の3段で実行し、pane の寿命を worktree の掃除ポリシーに連動させる。`Retain`/`Dirty` では pane を保持（F-23 の人間導線）。`Engine.released_panes: HashSet<task_id>` が release の一回性を保証（RPC が正常応答した時点で `released` の真偽によらず記録。削除が失敗し続けても再送しない。transport エラーは記録せず warn のみ = 次 sweep で再試行）。release 失敗は削除をブロックしない（孤児 pane は #211 doctor の受け持ち）。あわせて **worktree sweep の間隔を `SETTLE_TICK` から分離**: `sweep_finished_worktrees` は `Retained`/`DirtySkipped` 1 件ごとに `git status` のプロセス起動を伴い、200ms tick のままでは `keep_7d`/`keep_28d` の長期保持で毎秒 5 回走り続けるため、`EngineSettings.worktree_sweep_interval`（既定 60s・config 非露出、テストは `Duration::ZERO` で毎 cycle）で間引く。起動直後の初回 cycle は必ず sweep（起動時回収は即時のまま）、done 直後の cleanup（`finalize_success` 直呼び）は間引き対象外で pane close は即時。`settings_from_config` は `[worktree]` の保持プリセット **`keep_7d`/`keep_28d`**（#210）を `RetentionDays(7)`/`RetentionDays(28)` へ desugar する（`CleanupPolicy` は不変・既定値も不変） | #63 / #65 / #136 / #138 / #140 / #155 / #185 / #190 / #210 |
| `adapters` | ports の具象実装。`state_db`（SQLite 状態永続化・埋め込みマイグレーション・イベントログ・冪等取り込み・セッション永続化（`record_session`/`latest_session`/`list_sessions` F-37）→ [state.db スキーマ](/data/state-db.md)）、`run_lock`（多重起動防止 F-74）、`plugin_host`（プラグインをサブプロセス起動し NDJSON JSON-RPC で通信、initialize[task_source には PluginSpec.repositories = config.toml の [[repositories]] を供給 #109、PluginSpec.llm = config.toml の [llm]（api_key_ref 解決済み）を分類用 default として供給 #119]/protocol 互換チェック/リクエスト相関/タイムアウト/クラッシュ隔離/config·validate 委譲、F-51/54/58/59/65, §5.3。**#184（0.1.6）: プラグイン起点 request（P→O、`task/submit` 等）の受信** — reader が `method`+`id` を持つ行を `IncomingRequest { method, params, responder }` として `take_incoming_requests()` の channel へ配路（Notification 判定より**前**。従来はプラグイン起点 request が Notification として黙って破棄され id が失われていた）。応答は `Responder::ok/err` が共有 writer channel へ Response 行を書き戻すため O→P リクエスト行と交錯しない。プラグイン exit で channel close、孤児 Responder への応答は無害な no-op）、`agent_session`（`PluginAgentSession`＝`AgentSession` の実装、`session/attach`＋`state/subscribe` 再確立 F-37）、`hook_uds`（#136。UDS 受信サーバ driving adapter。`bind`＝stale ソケット unlink→`UnixListener` bind→0600 設定、`serve`＝接続ごと tokio::spawn・最小 HTTP/1.1 パーサ（`\r\n\r\n` までヘッダ→`Content-Length` body、chunked/method 非検査、path は**完全一致 `/focus` のみ**制御エンドポイントへルーティングし他は全てシグナル受信 E-08）・Bearer 定数時間検証 E-03・body>1MiB は 413・JSON→`AgentSignal` 正規化（未知フィールドは payload に温存、job_id 欠落/不正は 400 E-09）・`SignalPort::submit` 後即 200 E-04・**`/focus`（#155 F-94）は `{"task_id"}` を `FocusPort::focus` へ渡し `FocusOutcome` を JSON 応答**（[POST /focus](/apis/agent-events.md)）・watch シグナルで graceful 停止しソケット unlink）、`engine_signal_sink`（#136/#155。`EngineSignalSink`＝`SignalPort` + `FocusPort` 実装、`PluginEvent::HookSignal` を Engine のイベントチャネルへ転送する薄いラッパ（状態を持たず重複投入は素通し＝冪等は DB 層の責務 D-05）。focus は `PluginEvent::Focus { task_id, respond: oneshot }` を送り応答を await する request-response） | #48 / #51 / #57 / #136 / #155 |

## 公開型（#46 時点）

- `paths::Paths` — `from_system()` / `from_env(fn)`（テスト用に環境注入可）、各 `*_dir()` アクセサ。
- `ports::SecretString` — `Debug`/`Display` で値を露出しない newtype（§5.2 のログ流出防止）。`expose()` で明示的に取り出す。
- `ports::SecretRef` — `keychain:<service>/<account>` のパース済み表現。
- `ports::SecretStore` / `ports::ProcessProbe` — OS 依存機能の trait 境界。

# 依存

- `thiserror`（エラー型）。`libc`（unix、プロセス生存確認）。`keyring`（macOS 限定、Keychain）。
- 参照元: [orchestrator-cli](/components/orchestrator-cli.md)。

# 関連

- [ADR-0002 Rust workspace 構成と CI 品質ゲート](/decisions/adr-0002-rust-workspace-ci.md)
- [ADR-0004 フック完了シグナルの受信配置（ports::SignalPort + adapters::hook_uds）](/decisions/adr-0004-hook-completion-signal.md)
- [フックシグナルフロー](/architecture/hook-signal-flow.md) / [フックのセキュリティ](/security/hook-security.md) / [フックのトラブルシューティング](/operations/hook-troubleshooting.md)
- [POST /agent-events（UDS フック受信）](/apis/agent-events.md) / [state.db スキーマ](/data/state-db.md)
- [Spec §6 技術要件 / F-100〜F-107](/product/orchestrator-spec.ja.md)
