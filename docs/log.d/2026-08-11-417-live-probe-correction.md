* **Update**: **「`agent` は `pane.list` のレコードに載らない」は誤りだった**ので訂正した（#416 / #417）。dispatch 済みの workspace を herdr 0.7.5 で実測すると、エージェントの pane は `agent: "claude"` / `agent_status: "idle"`、伴走シェルは `agent: null` / `agent_status: "unknown"` を持つ。誤ったのは、**エージェントを起動していない probe workspace から推論して、dispatch 済みのものを測らなかった**ため。`looks_like_an_agent_pane` は 3 つとも読むようにし、テストの fixture も実機のレコードを転記した形に直した [agent-ide-herdr プラグイン](/components/agent-ide-herdr.md) / [herdr Socket API](/references/herdr-socket-api.md)
* **Update**: #417 の wire 一式を実機 herdr 0.7.5 で確認し、[ADR-0039](/decisions/adr-0039-herdr-sidebar-identity.md) に `verified` を付けた。`workspace.report_metadata` / `pane.report_metadata` が受理され、**`tokens` が `workspace get` と `pane list` の両方から読み戻せ**、**`workspace.rename` がトークンを保つ**ことを確認（ADR が主張していた点）。日本語のタイトルもそのまま往復する。`pane` の `label` は一連の操作の後も null のままで、#416 の前提が再確認された
