* **Creation**: **read-only profile の read-only 性を保証しないと決めた**（#446 を着手せずクローズ、[ADR-0045](/decisions/adr-0045-read-only-is-not-guaranteed.md)）。[ADR-0040](/decisions/adr-0040-worktree-sandbox-feasibility.md) が `sandbox-exec` での封じ込めを実現可能と実測していたが、**`git push` は sandbox でも止まらない**・macOS 限定（`sandbox-exec` は deprecated）・shim の配布が新しい運用面になる、という費用と限界から実装しない判断。強制力は deny の多層防御（`cat >` / `&&` / パイプで迂回可能）と D4 の事後検出（60 秒粒度・ブランチ単位）のままで、**エージェントがその気なら worktree を書き push できる既知の受容済みリスク**になった
* **Update**: 将来のサンドボックスを前提にしていた記述を 4 箇所直した — [ADR-0036](/decisions/adr-0036-read-only-violation-fails-the-task.md)（事後検出は「当面」ではなく最終形）、[ADR-0040](/decisions/adr-0040-worktree-sandbox-feasibility.md)（「実装は別 issue に切る」→ 実装しない。調査結果自体は有効）、[設定リファレンス](/development/config-reference.md)、および `hooks::permissions` / `run::enforce_read_only` の doc コメント（「The real boundary is a sandbox」「Prevention needs #418's sandbox」）
