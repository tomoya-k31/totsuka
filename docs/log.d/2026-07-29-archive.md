* **Creation**: [ADR-0022 docs バンドルを OKF v0.2 へ移行する](/decisions/adr-0022-okf-v02-migration.md) — OKF v0.2 公開に伴い、バンドル全体を v0.2 準拠へ移行した。破壊的変更 2 件（`timestamp` → `generated: { by, at }` / 本文 `# Citations` → frontmatter `sources`）と、拡張キーから標準キーになった `status` の語彙ずれ（`active` 48 件・`accepted` 21 件 → `stable`）を、新設した冪等な変換スクリプト `scripts/okf-migrate-v02.sh` で 73 ファイル一括変換した。複数リンクを含む引用行 5 箇所はスクリプトが WARN で列挙し、手で `sources` エントリへ分割した。
* **Update**: [ADR-0001](/decisions/adr-0001-adopt-okf.md) — 決定本文からバージョンのピン留めを外し、バージョン追従は ADR-0022 が担うことを明記した。
* **Update**: [okf-search](/components/okf-search.md) — `--after`/`--before` の比較対象を `generated.at` へ移し（`generated` が無い concept は旧 `timestamp` にフォールバック）、`--trust-tier`（§5.3 の信頼段階を `verified` から導出）`--stale` `--generated-by` `--source-like` を追加。出力列を `generated.at` と `trust` に変更した。ネストした frontmatter を読むため、字下げ行を直前のトップレベルキーの続きとして解釈するようにした。
* **Update**: [docs バンドル運用ルール](/CLAUDE.md) — v0.2 の frontmatter テンプレート、書ける YAML 部分集合（インデント 0/2/4）、actor 記法、`status` の 3 値、`sources` と脚注、`verified`/`stale_after` の記入トリガー、`Attested Computation` の扱いを追記した。`okf-lint` は YAML 部分集合をネスト対応へ広げ、v0.2 ファミリの意味検査（`status` 語彙・actor 記法・ISO 8601・`sources[].resource` 必須・旧 `timestamp`/`# Citations` の残存検出・`stale_after` 超過警告・脚注ラベル整合）を追加した。
