name = "orca"
kind = "agent_ide"
version = "0.1.0"
protocol_version = ">=0.1.0, <0.3"

# orca has no structured plan/preview API and no pane-control surface, so only
# the capabilities it can honestly fulfil are declared (F-33). design_preview
# and pane_control are intentionally omitted.
[capabilities]
plan_mode = true
state_stream = true
