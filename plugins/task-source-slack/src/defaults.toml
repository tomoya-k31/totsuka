# Built-in prompt defaults for the Slack task_source plugin (#318).
#
# Embedded in the binary with `include_str!` and parsed once at first use, so
# the prompt text is edited here rather than in Rust source (epic #311).
#
# These are DEFAULTS, not the live values: editing this file changes what a
# freshly built totsuka ships with, not what an existing installation uses.
# Override per installation under `[prompts]` in `plugins/slack.toml` — the key
# names here ARE the config keys (unlike the orca plugin, whose defaults are
# keyed differently from its flat config).
#
# `{placeholder}` substitution is single-pass, so a value containing `{token}`
# text is inserted, never re-expanded. That matters here more than anywhere
# else in totsuka: `{text}`, `{thread_context}` and `{catalog}` carry Slack
# content chosen by whoever wrote the message.
#
# Risk note: a bad override degrades classification (which falls back to the
# in-thread picker) or produces a weaker draft — it cannot break completion
# detection, unlike the core prompts.
#
# Most keys here are LLM-facing only, but `body_template` and the
# `body_attachment_*` / `body_thread_*` keys render the task body, which a
# **human** also reads in the pane. That is why they are the one group still written in Japanese while
# the instructions above them are English (ADR-0054): the instructions are
# plumbing, the body is what the operator looks at.

[prompts]

# Reply-crafting directions, delivered as `Task.instructions` (protocol 0.1.5)
# rather than in the body, so the host can inject them out-of-band while the
# pane shows only the mention and its thread context.
#
# Placeholders: none.
reply_instructions = """
Draft a reply to the Slack mention below. Investigate the target repository and answer with evidence. \
Write the reply in the same language as the thread. \
Output the reply text only, with no preamble, no postscript and no commentary. \
The asker is mentioned in front of your reply automatically: do not write any <@…> mention tag or `> ` quote marker yourself. \
The reply text is the only deliverable of this task. Do not change code, do not commit, and do not open a pull request. \
Even if you conclude that an implementation is needed, do not attempt it: write what should change and how, in the reply.\
"""

# Sent when the matched workflow's profile is `implement` (#398) — the reaction
# started an implement run rather than an answer.
#
# Selected by `instructions_kind`, **not** by the presence of a task-id prefix:
# `triage` carries one too (`books`), and picking on the prefix is the bug #450
# fixed.
#
# The reply still goes through the approval gate, so this asks for a report
# rather than a reply draft: the PR is the deliverable and its URL is the only
# way the thread learns it exists.
#
# Placeholders: none.
implement_instructions = """
Implement what this thread agreed on, then open a pull request. \
When you are done, output a report for the Slack thread. The report MUST contain the URL of the pull request you \
created — the orchestrator does not open pull requests, so this report is the only way the thread learns the URL. \
Write the report in the same language as the thread. \
Output the report only, with no preamble, no postscript and no commentary.\
"""

# Sent when the matched workflow's profile is `triage` (#450) — the `:books:`
# flow of #324: turn the thread into a backlog item rather than answering it.
#
# Selected by `instructions_kind`, not by the presence of a task-id prefix:
# `triage` and `implement` both carry one, and picking on the prefix is exactly
# the bug this key was added to fix (a triage run was told to implement).
#
# The deliverable is the issue, and — as with `implement` — its URL is the only
# way the thread learns it exists, so the report must carry it.
#
# "Do not change the code" is prose, and for `triage` **prose is the only thing
# saying it**: `triage` needs `Bash` for `gh issue comment`, so it cannot have
# the shell taken away, and read-only is not enforced anywhere (ADR-0045).
#
# Placeholders: none.
triage_instructions = """
Read this thread, work out what has to be done, and file it as a GitHub issue. Do not change the repository's code. \
When you are done, output a report for the Slack thread. The report MUST contain the URL of the issue you filed — \
the orchestrator does not file issues, so this report is the only way the thread learns the URL. \
Write both the issue and the report in the same language as the thread. \
Output the report only, with no preamble, no postscript and no commentary.\
"""

# Appended to `reply_instructions` only when `reply_style` is set in
# `plugins/slack.toml`. `reply_style` itself stays a plain user string — it is
# content, not a template.
#
# Placeholders: {style}
reply_style_suffix = """

Reply style: {style}\
"""

# The visible task body: what the pane shows.
#
# Placeholders: {sender} {channel} {text}
#
# `{text}` arrives ALREADY `>`-quoted — the newline-to-`\n> ` rewrite happens in
# Rust before substitution. So a template that drops the leading `> ` still
# gets sane continuation lines, and one that keeps it must not add a second
# level of quoting.
body_template = """
## メンション

- 送信者: {sender}
- チャンネル: #{channel}
- 本文:

> {text}
"""

# Header for the attachment section, emitted only when the message carried
# files.
#
# Placeholders: {count}
#
# The "content is not in this task" sentence is load-bearing, not decoration:
# the plugin has no `files:read` scope, so it can name a file and nothing more.
# An
# agent handed "md ファイルにしました" with the file erased answered as if the
# message had no attachment; this section is what stops it inventing the
# content instead of saying it could not read it.
#
# **The permission to fetch it anyway is equally load-bearing**, and was added
# after production showed why: an agent whose Slack MCP was configured parsed
# the file id out of the permalink on the line below and read the attachment
# itself — under the `answer` profile, which denies the shell entirely. The
# first wording listed only 名前・種類・サイズ and told the agent to reply that
# it could not read the file, which steers away from the one path that works.
# So: say what the task carries, allow the agent its own route, and keep the
# "do not guess" instruction for the case where that route is unavailable.
#
# **Nothing agent-facing names the orchestrator.** The agent is handed a
# mention and its thread; "totsuka" is not in that world, so a sentence whose
# subject is totsuka says nothing to the reader. This file and every other
# prompt default were free of the word — an earlier draft of this key was the
# single exception, and it is stated here so the next edit does not reintroduce
# it.
#
# No tool is named on purpose. `slack_read_file` is one route among several and
# naming it sends an agent that does not have it looking for a tool that is not
# there.
#
# The permission is **conditional on a link actually being shown**, because
# `SlackFile.permalink` is an `Option` and the line omits it when Slack did not
# supply one (a tombstone file, for instance). Promising a link that is not
# there would point the agent at a handle it cannot find.
body_attachment_header = """

## 添付ファイル（{count} 件）

（添付ファイルの中身はこのタスクに含まれていません。分かっているのは以下の行に出ている情報だけです。リンクが付いている添付は、中身が必要なら、そこから自分で取得できる手段があれば使ってください。取得できなかった場合や、リンクが付いていない添付については、推測で補わず、添付を読めていないことを返信に書いてください）

"""

# One attachment line. Rendered once per file.
#
# Placeholders: {file}
#
# `{file}` arrives fully composed (name, MIME type, size, permalink) because
# every part but the name is optional — same reason `body_thread_line` takes a
# whole `{line}`.
body_attachment_line = """
- {file}
"""

# Header for the thread-context section, emitted only when context lines were
# fetched.
#
# Placeholders: {count}
body_thread_header = """

## スレッド文脈（直近 {count} 件・古い順）

"""

# One thread-context line. Rendered once per line.
#
# Placeholders: {line}
body_thread_line = """
- {line}
"""

# Substituted for the whole thread-context section when the fetch failed.
# Distinct from "the thread has no other messages", which emits nothing.
#
# Placeholders: none.
body_thread_unavailable = """

## スレッド文脈

（スレッド文脈の取得に失敗したため省略されています）
"""

# System prompt for the repository classifier, used when channel rules leave
# more than one candidate.
#
# Placeholders: {repo_names}
#
# The literal `{"repo": string, ...}` below is JSON output shape, not a
# placeholder: `"repo"` is not a bare identifier, so the single-pass renderer
# emits it verbatim. (In Rust this had to be written `{{"repo": ...}}` to
# escape `format!`; here it is literal.)
classifier_system = """
You classify which local repository a Slack mention is about. Answer with ONLY a JSON object of the exact shape {"repo": string, "confidence": number, "reason": string} — no prose, no code fences. `repo` MUST be one of: {repo_names}. `confidence` is 0.0-1.0, your own estimate of how sure you are.\
"""

# User message for the classifier.
#
# Placeholders: {mention_text} {thread_context} {catalog}
#
# All three are attacker-controllable Slack content. Single-pass rendering is
# what keeps a mention containing the literal text `{catalog}` from splicing
# the candidate list in.
classifier_user = """
## Mention
{mention_text}

## Thread context
{thread_context}

## Candidate repositories
{catalog}\
"""

# Retry turn appended after a malformed answer.
#
# Placeholders: none.
classifier_correction = """
That response was not a parseable JSON verdict. Answer again with ONLY the JSON object — no prose, no code fences.\
"""
