> 🌐 [English](SECURITY.md) · **日本語**
> _英語版が正(canonical)です。差分がある場合は英語版を参照してください。_

# セキュリティポリシー

## サポート対象バージョン

totsuka は 1.0 前で、単一の系列からリリースしています。セキュリティ修正は
**最新リリースのみ**が対象で、バックポート用のブランチは維持していません。
現行バージョンは
[リリースページ](https://github.com/tomoya-k31/totsuka/releases)を参照してください。

## 脆弱性の報告

**セキュリティ上の問題を public issue に書かないでください。** 代わりに、
下記の非公開の経路から報告してください。

GitHub の
[private vulnerability reporting](https://github.com/tomoya-k31/totsuka/security/advisories/new)
から非公開で報告してください。個人が余暇で保守しているプロジェクトのため、
初回返信までに数日いただくことがあります。

報告に含めていただけると助かる情報: バージョン、プラットフォーム、関係する設定
（シークレットは伏せてください）、再現手順。

## スコープ

totsuka は運用者のマシン上でローカルに動作し、連携先サービス（GitHub、Slack、
Notion、LLM エンドポイント）の資格情報を保持し、git worktree 上でコードを実行する
AI エージェントへディスパッチします。とくに対象となる報告:

- **資格情報の漏洩** — 本来渡るべきでないログ・エラーメッセージ・端末・
  サブプロセスの環境変数へシークレットが到達するもの。
- **信頼できない入力が特権的なシンクへ到達する経路** — タスク文面は外部
  （Slack メッセージ、GitHub issue）由来なので、その経路を通じたコマンド
  インジェクション・引数インジェクション・端末エスケープシーケンス注入。
- **サンドボックスや境界の突破** — タスクが自身の worktree の外へ出る、
  プラグインがプラグインプロトコルの外へ手を伸ばす、といったもの。
- **承認ステップを経ずに運用者本人の名義で publish される**もの。

## 既知の非目標

以下は意図的な設計判断であり、脆弱性ではありません。脆弱性としての報告は
ご遠慮ください。

- **read-only 実行は明示的に保証しません。** read-only profile
  （`answer` / `triage` / `design`）や `mode = "plan"` は意図の表明であり、
  `deny` による多層防御と事後検出は入りますが、エージェントが Bash 経由で
  ファイルを書き・commit し・push し・PR を作ることを構造的に止めるものでは
  **ありません**。サンドボックス化は検討のうえ意図的に不採用としました
  （→ [ADR-0045](ai-docs/decisions/adr-0045-read-only-is-not-guaranteed.md)）。
- **エージェントは worktree について信頼されている。** totsuka は AI エージェント
  にチェックアウトを渡して作業させます。その中で望ましくない動作をするよう
  エージェントを説得するプロンプトは、エージェント側の性質であって totsuka の
  境界の問題ではありません。
- **シークレットの秘匿性は、それを保持するバックエンドの性質どまり。** totsuka は
  参照（Keychain、`op://`、`cmd:`、env）を保存し、自身の設定ファイルへ値を書く
  ことはありませんが、手で設定ファイルに貼られたシークレットはそこに残ります。
