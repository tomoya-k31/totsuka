* **Update**: [task-source-github](/components/task-source-github.md) / [設定リファレンス（config.toml）](/development/config-reference.md) — #514: GitHub トークンの権限を実測できる形にし、**docs の誤りを 1 件直した**。**fine-grained PAT の Account permissions に Projects は存在しない**（[GitHub の権限一覧](https://docs.github.com/en/rest/authentication/permissions-required-for-fine-grained-personal-access-tokens) が挙げる User permissions に無く、Projects は Organization permissions にしかない）。したがって「user 所有なら Account permissions」と書いていた行は**存在しない権限を指していた** — user 所有ボードでは fine-grained PAT で ProjectsV2 に到達できず、classic PAT の `project` scope を使うしかない。読者が最初にぶつかるのはこの分岐なので、権限表より前に「所有者 → 使えるトークン種別」の表を置いた。あわせて `.claude/skills/live-e2e/scripts/github-permissions.sh` を追加した。プラグインが実際に投げる 4 操作（`fetch_query` / `resolve_query` / `UPDATE_STATUS_MUTATION` / `VIEWER_QUERY`）を**同じエンドポイント・同じヘッダ・同じクエリ本文**で送る。`doctor --online` は `viewer` しか叩かない（F-59）ので**doctor が緑でも fetch が空になる権限構成が存在**し、逆に実 `run` は LLM・herdr・worktree が同時に動くので権限だけを切り分けられない。その隙間を埋めるためのもの。**設計上の要点は「エラーが出なかった」を pass と読まないこと**: GraphQL の権限不足は HTTP 200・`data` あり・フィールドが `null` という形で出うるので、`errors` の有無と独立にフィールド単位で present/null を判定する。`assignees` / `labels` は **`nodes: null`（権限不足の疑い）と `nodes: []`（本当に付いていない）を別物として数え**、後者は「測れなかった」として skip（終了コード 3）で pass と区別する。`--write` は item の**現在の Status と同じ option を書き戻す**ので、write 権限を測りながら盤面を動かさない（`update_status` 自身が冪等と明記しているのと同じ性質）。実測できたのは**十分条件だけ**で、OAuth トークン（scope `gist, project, read:org, repo, workflow`）が 4 操作すべてを通し、`title` / `body` / `url` / `number` / `repository.name` が 62/62 件で非 null、`assignees` / `labels` も `nodes` が null にならないことを確認した。**最小値は未実測**なので「導出であって実測ではない」という但し書きは残してある（消すのは実測が済んでから）。残る最大の問い —— Issue の中身は `projectV2` 経由でしか読まないので `project` scope だけで返るなら `repo` は要らない —— は、`project` だけの classic PAT を 1 本切れば 1 回で決着する。サンドボックスは user 所有なので、**fine-grained PAT 側の最小値はこの環境では測れない**（org 所有の Project が要る）。
