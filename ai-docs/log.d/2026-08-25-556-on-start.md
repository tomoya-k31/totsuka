* **Update**: [orchestrator-core](/components/orchestrator-core.md) / [config-reference](/development/config-reference.md)（#556 / [ADR-0059](/decisions/adr-0059-task-claim-exclusion.md)）。`[[workflows]].on_start = { set_status = "…" }` を新設: dispatch 直前にソース側ステータスを書き戻し、ボードが「実行中」を映して `in_progress_statuses` の取り込み除外が第 2 防御線として自動で効くようになる。未設定なら何も書かない（既存 config の挙動は不変）。`write_back_status` の `success: bool` は 3 値の `StatusMoment`（Start/Success/Failure）へ。
