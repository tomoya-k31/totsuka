* **Update**: [config-examples](/development/config-examples.md) の `location` プレースホルダ一覧を更新した（#645 のレビュー指摘）。`config-reference` だけ直して例のほうを取り残しており、しかも「これ以外を書くとバリデーションエラーになる」と続く文脈だったので**積極的に誤り**だった（`okf-lint` はバンドル内の記述矛盾を見ないので CI では落ちない）。あわせて [orchestrator-core](/components/orchestrator-core.md) の `template` 行に残っていた削除済み `render_worktree_name` の記述を直し、**`render_location` がマルチパスであることが #645 以降 load-bearing になった**ことを明記した — ソース由来で生のまま埋める `{task_id}` / `{source}` を**最後に**置換することで、`{hash}` のような文字列を含む task id が後段に書き換えられないようにしている（回帰テスト `a_placeholder_inside_a_task_id_is_not_re_substituted` を追加）。
