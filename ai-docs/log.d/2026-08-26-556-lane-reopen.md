* **Update**: [task-source-github](/components/task-source-github.md) / [orchestrator-core](/components/orchestrator-core.md) / [config-reference](/development/config-reference.md)（#556 / [ADR-0059](/decisions/adr-0059-task-claim-exclusion.md) §5）。スイムレーン差し戻しによる再実行を実装: `project_status` トリガーの配送に `message_key = "status:{列名}@{セルの updatedAt}"` を刻み、#242 の会話再開に乗せる（コアの reopen 経路は新設ゼロ）。level でなく edge — サーバー発行タイムスタンプの等値比較だけで、毎 tick 再配送も完了直前の遅延スナップショットも dedup。label-only トリガーは対象外。ingest に**別ワークフロー配送の破棄ガード**（警告つき・台帳に書かない）、validate に **`set_status == 自分の trigger.project_status` のエラー**（字面一致のみ）を追加。アップグレード時、トリガー列に置き去りの完了カードは初回 poll で 1 回だけ reopen する。 [ADR-0059](/decisions/adr-0059-task-claim-exclusion.md) の Status を「全 6 PR 実装済み・実機検収待ち」へ更新。
