* **Update**: `[worktree].location` の新プレースホルダ（`{task_number}` / `{hash}`）を **`config::validate` の許可リストにも入れた**（#645 のレビュー指摘）。描画側だけに足していたため、それを書いた設定は `totsuka config validate` の段階で「未知のプレースホルダ」として弾かれ、描画に到達しなかった。許可リストと描画は 1 つの契約の 2 つの半分で、**片方だけ動かすと壊れる**ことを定数の doc に明記し、両方を通す回帰テストを足した。あわせて [orchestrator-core](/components/orchestrator-core.md) の `worktree` 行に残っていた削除済み API（`DEFAULT_WORKTREE_NAME_TEMPLATE` / `render_worktree_name` / `sanitize_branch_for_path`）の記述を書き直した。
