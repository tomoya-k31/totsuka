* **Update**: [agent-ide-orca](/components/agent-ide-orca.md) の dispatch が、orca が worktree を認識するまで `worktree show` で待つようにした（最大 20 秒）。orca 版の実機 e2e で、Orchestrator が作り直した worktree を orca が約 10 秒認識せず、dispatch が「リポジトリ未登録」の誤った案内で 2 回失敗した（自動再試行に救われた）。新しい worktree の発見の遅れ（0.8〜2.1 秒）と、閉じた端末の `worktreePath: ""` を [orca CLI 制御サーフェス](/references/orca-cli-control.md) の実測節に記録し、未登録エラーには orca の元の応答を添えるようにした
