* **Creation**: [ADR-0083](/decisions/adr-0083-repo-classifier-crate.md) と [repo-classifier](/components/repo-classifier.md) を追加（#723）。リポジトリ分類を core の `LlmRouter::chat_json`（通信手段で切った抽象）から、タスクと候補を渡すと検証済みの判定が返る `RepoClassifier` に切り直し、Slack プラグインと二重だった HTTP・再試行・エラー整形・chat 実装を leaf クレートにまとめた。挙動は不変で、decisions 方式（TypeSafe Jev）は後続 PR でこの切れ目に載る。
* **Update**: [orchestrator-core](/components/orchestrator-core.md)・[task-source-slack](/components/task-source-slack.md)・[orchestrator-cli](/components/orchestrator-cli.md) の分類まわりの記述を新しい型に合わせ、[依存境界ルール](/architecture/workspace-dependency-rules.md) に `repo-classifier`（plugins から使える leaf）を足した（#723）。
* **Update**: [設定リファレンス](/development/config-reference.md) に `[llm].confidence_threshold`（既定 0.6、F-14）を追加し、[設定例](/development/config-examples.md) に `TOTSUKA_LLM_CONFIDENCE_THRESHOLD` を足した。`[llm].max_tokens` の既定値表記を実際の挙動（省略時は送らない）に合わせて直した（#723）。[仕様](/product/orchestrator-spec.md) の F-14 と ports の記述も更新。
