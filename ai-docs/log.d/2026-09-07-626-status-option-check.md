* **Update**: [ADR-0069](/decisions/adr-0069-workflow-projects.md) §7 / [task-source-github](/components/task-source-github.md) / [task-source-notion](/components/task-source-notion.md) / [設定リファレンス（config.toml）](/development/config-reference.md) — #626: **ボードに存在しない `status` を名指す設定を `config validate` のオンライン部と `doctor` で error にした。** `projects` で走査範囲を絞っても綴り違いは「無言で 0 件」のまま残る —— 走査先を絞ることは検査ではないので、対で入れる必要があった。対象は `trigger.status` / `on_start` / `on_success` / `on_failure` / `[[projects]].triage_status` で、`in_progress_statuses` は全 domain 共通のため対象外。`initialize` では落とさない（列を 1 つ消しただけで無関係なワークフローまで止まる）
* **Update**: [plugin-protocol](/components/plugin-protocol.md) — プロトコル 0.7.0 に `WorkflowInfo.status_writebacks` を追加（`on_*` から core が導出した列名の重複なしリスト）。`on_*` のテーブル自体は渡さない —— ボードの option と突き合わせられるのはプラグインだけだから、その 1 点のために導出値だけを送る。あわせて `WorkflowInfo.projects` を agent へ送らないよう直した（doc は「agent には空」と書いていたのに無条件に送っていた）
