* **Update**: [ADR-0061](/decisions/adr-0061-workflow-handoff.md)（#568）。修正後の再検収が完了したので `verified` を戻した。実データ: 質問待ちのエージェントが承認まで 3 分 54 秒生存（修正前は質問提示の 3 秒後に pane を閉じられていた）／`read_only_violation` 0 件／worktree が掃除で消えていた経路（detach ではなく `branch` 列のクリアが効く側）を通した／掃除は 3 回とも `Removed` で、到達可能性判定は `0` を返し `Dirty` 回帰なし／前段の PR とブランチは無傷／design → implement の往復も完走し会話 uuid は同一。
