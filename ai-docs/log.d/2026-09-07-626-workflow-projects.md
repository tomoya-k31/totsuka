* **Creation**: [ADR-0069 workflow は source ではなく projects で domain を名指す](/decisions/adr-0069-workflow-projects.md) / [Project（プロジェクト / domain）](/glossary/project.md) — #626: `[[workflows]].source` を廃止し `projects`（`[[projects]].name` の配列・必須）へ置き換え、タスクソースは `[[projects]].source` から導出する。同一 source の複数ボードで Status の option 集合が違う構成が「取り込みは無言で 0 件・書き戻しはハードエラー」だった原因を、走査範囲を workflow の列挙に絞ることで消した
* **Update**: [設定リファレンス（config.toml）](/development/config-reference.md) / [設定サンプル集](/development/config-examples.md) — `[[projects]]` の意味を「リポジトリの起票先トラッカー」から「**ソースが持つ domain**」へ広げた（#626）。2 方向から指される: `[[workflows]].projects` が取り込み元として（配列）、`[[repositories]].project` が起票先として（スカラー）。domain を持たない slack / discord も `name` + `source` の 2 行を書く
* **Update**: **列閉路検査のグラフを `(domain, 列名)` でキーするようにした**（#626）。それまでは `source` 単位で、同一プラグインの 2 枚のボードが同じ節点になっていた —— ボード A の `Done` とボード B の `Done` が繋がり、**走らない閉路が報告されえた**。設定リファレンスは以前からこの性質を主張していたので、実装が記述に追いついた形である
* **Update**: [plugin-protocol](/components/plugin-protocol.md) / [plugin 開発ガイド](/development/plugin-dev-guide.md) — プロトコル **0.7.0**: `WorkflowInfo.projects` を追加。フィールドの追加だが `WorkflowInfo` は `deny_unknown_fields` ではないので、旧ビルドは無視して全 domain を走査する（無言の誤スコープ）。0.6.0 の `triggers` 改名と同じ扱いで F-54 のゲートに拒否させる。**下限を上げるのは domain が複数ある github / notion だけ**で、残る 5 本は上限のみ `<0.8` へ
* **Update**: [task-source-github](/components/task-source-github.md) / [task-source-notion](/components/task-source-notion.md) — fetch が `WorkflowInfo.projects` で名指されたボード / データベースだけを走査する（#542 の「全ボード走査」を #626 が workflow の列挙に絞った）
* **Update**: [orchestrator-core](/components/orchestrator-core.md) / [orchestrator-cli](/components/orchestrator-cli.md) — `Workflow.projects` と導出値の `Workflow.source`（`from_config` に相乗り）。`totsuka setup` はレシピが入れる task_source ごとに `[[projects]]` を 1 本吐く
* **Update**: [Workflow（ワークフロー）](/glossary/workflow.md) / [Task Source（タスクソース）](/glossary/task-source.md) — 定義の第一要素が `source` から `projects` になり、プラグインとの対応は `[[projects]].source` 経由になった
