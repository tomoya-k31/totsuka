* **Creation**: [ADR-0082](/decisions/adr-0082-orca-herdr-parity.md) — orca プラグインを herdr と同じ契約で駆動する。`worktree create` で 2 本目の worktree を作り、独自の `--agent` 起動と state dot の poll で完了を判定していたのをやめ、Orchestrator の worktree に `orca terminal create` で端末を開いて `tool_launch` をそのまま起動する（`exec env …`）。完了は hook、state stream は `terminal wait --for exit` の deadman。`pane_control` / `hook_completion` / `diagnostics_snapshot` を宣言する。変更前のプラグインは orca の `--json` envelope のトップレベル `id`（CLI リクエストの id）を worktree id と取り違えており、実機では動いていなかった
* **Update**: [agent-ide-orca](/components/agent-ide-orca.md) を新しい構成（`cli` の envelope 解釈・`launch`・廃止キーの墓標・メソッド写像）で書き直した。`[orca]` は `orca_bin` / `request_timeout_secs` / `[orca.layout]` / `[orca.identity]` になり、`agent` / `setup` / `repo_selector` / `plan_prompt_prefix` / `poll_interval_ms` は `initialize` がキー名と代替を挙げて拒否する。**対象リポジトリの `orca repo add` が前提**になった
* **Update**: [orca CLI 制御サーフェス](/references/orca-cli-control.md) に orca 1.4.205 の実測節を足した — `--json` envelope とエラーコード、`--command` が打ち込みであること、`--title` が初期値にすぎず `rename` だけが保たれること、`tui-idle` の時点ではまだ `agentIdentity` が無く、そこで送ったプロンプトが Claude に届かないこと、`exitCode` が実際の終了コードを反映しないこと。`stale_after` を付けた
* **Update**: orca を「hook 非対応」「`pane_control` 非宣言」として説明していた箇所を直した — [pane](/glossary/pane.md)・[click-to-focus セットアップ](/operations/click-to-focus-setup.md)・[設定例](/development/config-examples.md)・[プラグイン開発ガイド](/development/plugin-dev-guide.md)・[リリース前チェックリスト](/quality/release-checklist.md)（orca 節を新しい確認項目に差し替え）
