* **Verified**: [ADR-0065](/decisions/adr-0065-menubar-status.md) / F-109 / F-110（2026-08-28）。メニューバー表示を実機（SwiftBar 2.1.1 / macOS）で検収完了した。**`⚠` の発火は隔離した XDG で取った** —— `run --watch` を起こし hook receiver の bind を失敗させた状態で、`health.json` に `hook_receiver_down` が publish され、`totsuka status` の `degraded:`・`status --json` の `health`・`totsuka menu` の `⚠` が同じ記録を読むところまで一本で確認した。**この機能の存在理由そのものの状態である** —— `status` は `running (pid …)` と出てプロセスは元気なまま、完了検知だけが死んでいる。あわせて **graceful shutdown で `health.json` が消え `⚠` が `✕` に戻る**ことも確認した（ユニットテストでしか押さえていなかった経路）。実運用の設定は一切触らず、`XDG_*` を全て隔離ディレクトリへ向けて行った。

* **Note**: 実機でしか出なかった不具合が **3 つ**、実機でしか否定できなかった記述が **1 つ**あった —— 改行エスケープの無効化（1 行が 3 行に割れる）、`:name:` 展開による文字の消失、「config 不正も 1 行になる」の誤り、そして「GUI 起動は最小 `PATH`」の誤り（**mise shims は実際には入っていた**）。**4 件すべてユニットテストは全通過していた。** 共通するのは外部システム（SwiftBar / macOS の PATH 解決）の挙動を測らずに書いたことで、自分のコードの範囲（`|` のエスケープ・要対応の集合・`run.lock` 優先）には同種の誤りが 1 件も出ていない。**外部の挙動は、書く前に測るか upstream を読む。**
