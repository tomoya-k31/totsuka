* **Update**: [orca CLI 制御サーフェス](/references/orca-cli-control.md) と [agent-ide-orca](/components/agent-ide-orca.md) に、totsuka の worktree が orca の GUI でどう見えるかを追記した。orca から見ると external worktree で、リポジトリ設定 `externalWorktreeVisibility` が `show` ならサイドバーのプロジェクト配下に表示され、エージェントのタブも見える（GUI で確認）。CLI では `worktree list --repo` には出るが、`--repo` 無しの `list` と `ps` には出ない。[リリース前チェックリスト](/quality/release-checklist.md) の前提にこの設定を足した
