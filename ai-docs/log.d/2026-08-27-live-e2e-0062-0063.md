* **Verified**: [ADR-0062](/decisions/adr-0062-status-vocabulary.md) / [ADR-0063](/decisions/adr-0063-trigger-assignee.md)（2026-08-27）。実機（実 GitHub ProjectsV2 + 実 herdr + 実 Claude Code）で検収した。**旧綴りが無言にならないこと**を 2 層で確認: 実運用と同形の設定（`[[workflows]]` 5 本）を旧綴りのまま `config validate --offline` に掛けると `on_*` の 3 箇所を**すべて**名指しして exit 1、`on_*` だけ直して `run` すると github プラグインが `initialize` を `-32003` で落とし `project_status` を名指しして有効キー 3 つを列挙する（**プロセス全体が起動しない**）。**新綴りが実際に書き戻すこと**も確認: `on_start` と `on_success` の両方がカードを動かした。#572 は**同じ issue で assignee だけを変える A/B** で取った —— 片側だけだと「fetch の空振り」と区別できないため。`Todo` 列の**未アサイン** issue は 2 ポーリングを超えて取り込まれず、`tomoya-k31` を付けた 73 秒後に dispatch された。claim の縮退（同一ログインでは書き込みなしの `Won`）も、取り込み後に assignee が 1 人のままであることで確認。**notion は e2e のロスターに入っていないので実機検収の対象外**であり、notion 側の変更（`assignee` 条件・`status` 綴り・at-most-once）は単体/結合テストまでの担保である。
