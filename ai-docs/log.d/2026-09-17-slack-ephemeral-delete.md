* **Update**: [task-source-slack](/components/task-source-slack.md) / [ADR-0074](/decisions/adr-0074-single-draft-surface.md) / [ADR-0021](/decisions/adr-0021-slack-bot-notification-nudge.md) / [エフェメラル承認フロー](/glossary/ephemeral-approval.md) — 承認・却下ボタンを押してもエフェメラルが残る件に、**削除で**応えた。決定 2（置換して残す）は「消すと却下の痕跡がどこにも残らない」という制約から出た結論であって、置換が望ましいという判断ではなかった。制約のほうを外す —— ナッジ DM（#305）は #456 で既に返信案本文をログとして持っているので、押下時にそこへ ✅/❌ を `chat.update` で書き戻し（bot が自分のメッセージを編集するので通知は増えない）、**それが成功したときだけ** `response_url` へ `delete_original` を送る。順序が全部で、逆にすると失敗した `chat.update` が唯一の面ごと決定を消す。書き戻す先が無い構成（`bot_token` 未設定 / bot DM 未解決 / ナッジ投稿の失敗 / `nudge_ts` を持たない旧下書き / `chat.update` の失敗）は決定 2・3 の置換にフォールバックするので、`bot_token` を使っていない運用の見え方は変わらない。`Draft.nudge_ts` は `#[serde(default)]` の任意フィールドで、`drafts.json` のスキーマ bump も移行作業も不要（ADR-0021 が「価値が無い」と断じた永続化はここで価値を得た）。ADR-0074 に決定 7、ADR-0021 に該当 2 項目の撤回を追記。
* **Update**: [ADR-0074](/decisions/adr-0074-single-draft-surface.md) / [ADR-0021](/decisions/adr-0021-slack-bot-notification-nudge.md) — 上記のレビュー指摘 2 件を反映。(1) ナッジへの ✅/❌ 記録は `response_url` の**有無より先**に行う —— 記録と面の片づけは別の問いで、URL が要るのは後者だけなのに両方を門番していたため、URL 無しの配送（契約上あり得る）で「ボタンは残り、DM も『返信案が届きました』のまま」になっていた。(2) ADR-0021 の該当項目は、撤回を下に足すだけでなく**見出し自体を現行の規則に書き換えた**（「削除しない。更新は下書きのナッジだけ」）—— 太字の一行目が古い規則のままだと、拾い読みする人に逆の結論が伝わる。
* **Update**: [エフェメラル承認フロー](/glossary/ephemeral-approval.md) — `/code-review` の指摘（決定 7 で書き換えたはずの記述が同じファイルに旧版のまま残っている型、Copilot が ADR-0021 で挙げたのと同型）を反映。用語集の第 2 段落が「「承認したのか却下したのか」は残らない」と第 1 段落と正反対のことを言っていたので訂正し、「二重押下も塗り直す」も現行の後始末（記録 → 削除、できなければ置換）に合わせた。
