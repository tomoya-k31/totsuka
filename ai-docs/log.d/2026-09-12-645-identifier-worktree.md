* **Update**: [ADR-0071](/decisions/adr-0071-task-identifier-naming.md) の D-6 を実装に合わせて書き直した（#645 の 2 本目）。葉の既定を差し替えるだけのつもりだったが、**worktree 名のテンプレート機構ごと削除**している — `{source}-{task_id}` の git ref 合法化は葉がブランチ名だった頃の遺産で [ADR-0026](/decisions/adr-0026-agent-owned-branch-and-push.md) 以降その前提は無く、ポリシーの出力 `[A-Za-z0-9_-]` は git ref 規則にもパス要素規則にも厳密に収まるので合法化に仕事が無い。加えて `worktree_name_template` は `config.toml` から到達できず既定値しか取らない設定で、残したまま葉をポリシー化すると「3 ツールが同じ core を持つ」性質がテンプレート文字列の偶然の一致になる。
* **Update**: [orchestrator-core](/components/orchestrator-core.md)（`worktree` 行の命名記述と `location` の新プレースホルダ `{task_number}` / `{hash}`）、[agent-ide-orca](/components/agent-ide-orca.md)（`WorktreeName` と、**orca が `--name` の制約を一切公開していない**ため宣言を意図的に狭く取っていること）、[config-reference](/development/config-reference.md)（`location` の展開一覧と `{worktree_name}` の新しい意味。`{task_id}` は据え置き）を更新した。
