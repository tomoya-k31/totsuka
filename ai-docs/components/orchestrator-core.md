---
type: Component
title: orchestrator-core クレート
description: totsuka のコア。ヘキサゴナルアーキテクチャの domain（ドメイン・ステートマシン）/ ports（TaskSource・AgentIde・LlmRouter・SecretStore 等の trait）/ adapters（JSON-RPC ブリッジ・SQLite・Keychain）を担う。
resource: https://github.com/tomoya-k31/totsuka/tree/main/crates/orchestrator-core
tags: [rust, crate, core, hexagonal, xdg, platform, config, sqlite, statemachine, logging, plugin, worktree, git, workflow, scheduler, llm, repo-select, recovery, session, run, output, signal, hook]
generated: { by: claude-code/opus-5, at: 2026-08-25T21:00:00+09:00 }
verified:
  - { by: human:tomoya-k31, at: 2026-08-20T10:30:00Z }
status: stable
owner: tomoya-k31
---

# 責務

totsuka のビジネスロジックの中核。外部 I/O を持たず、ports の trait 境界を介してのみ外界とやり取りする（ヘキサゴナル）。OS 依存機能は `platform` に隔離する（§5.6）。

# モジュール構成

| モジュール | 責務 | 実装タスク |
|---|---|---|
| `domain` | 純粋なドメイン型。**#415: `Workflow.initial_prompt`**（`[[workflows]].initial_prompt` を解釈したもの。`from_config` が空文字列・空白のみを `None` へ正規化する唯一の場所 — 下流が trim を思い出さなくて済むよう解釈の 1 箇所に寄せた → [ADR-0038](/decisions/adr-0038-workflow-initial-prompt.md)）。`state`（`transition(from,event)` 純関数、11 状態、F-71。#133 で `Verifying`（human 検収待ち、#131 D-01）/ `Escalated`（人間対応待ち、D-02/D-03。どちらも非 terminal）とイベント `SelfReportComplete`/`ApproveVerification`/`VerificationFailed`/`Escalate` を追加 — Escalate は全非 terminal から到達、Escalated からは次シグナルで Verifying/Publishing/WaitingInput/Running へ復帰。**#256 (#242) で `Reopen` を追加** — `(Done|Failed|Cancelled) → Queued`。`Retry`（同じ指示をやり直す）とは別イベントにしているのは意味が違うためで、`Reopen` は**新しい指示が来た**（会話の継続）を表し、`Done` から出られる唯一の経路でもある。これにより `Done` の意味が「永久に完了」から**「未処理メッセージが無い」**へ変わり、「終端は不可逆」という不変条件を意図的に手放す。`is_terminal()` は「今やることが無い」の意味に留まり、Escalate/Fail/Cancel のガード・worktree 掃除候補・スロット計上のいずれとも競合しない（Reopen 後は Queued なので掃除候補から外れ、スロットも非計上）。なお `events` は from_state/to_state のみを記録するため **Retry と Reopen は `failed → queued` として区別できない**（`done → queued` だけが一意）。由来が必要な呼び出し側は `apply_event` の `detail` に入れる規約とする（`totsuka task retry` が既に `{"kind": "cli", ...}` を入れている先例に倣う））、`signal`（#133。フックシグナルの正規化型 `AgentSignal`/`SignalSource`/`SignalEvent`（Stop/Notification/QuestionPending/SessionStart/SessionEnd/Heartbeat。QuestionPending は #487 — 質問ダイアログ待機中は Stop が届かないため、Notification と違い WaitingInput へ park する）/`StopStatus`/`JobId`（`"job-{task_id}-{session_row}"`、FromStr/Display、不正形式は `InvalidJobId`）。**#178: マーカー提示形の共有定数 `MARKER_COMPLETED`/`MARKER_NEEDS_INPUT`/`MARKER_FAILED`** — **#313 以降、両 instruction の文面は `prompts` の `defaults.toml` に移り、ここは `{marker_*}` プレースホルダの解決先としてワイヤ規約の Rust 側定義を一元化する役割に絞られた**（マーカー自体は設定不可）。UDS 受信サーバ＝`adapters::hook_uds`（#136 実装済み）、Engine 配線＝`run::hooks::Engine::on_signal`（#138 実装済み。`hook_uds::parse_signal` はスプール回収でも再利用））、`workflow`（`source×trigger×mode×agent×output` 定義の解釈・検証（output=source の capability 検証 F-83。plan×pull_request の F-82 チェックは `pull_request` 自体の廃止で対象消滅）・on_success/on_failure=OutcomeAction F-84）。**#554 でトリガーマッチングごと無くなった** — 定義順 first-match（F-81）を走らせるのはソースプラグインで、`Trigger` はここでは `initialize` へ渡す不透明なテーブルでしかない。`Trigger::matches` と予約キー（`status` / `project_status` / `label` / `labels` / `reaction`）、`match_workflow`、trigger 重複警告と catch-all 到達不能検出は削除した | #48 / #54 / #133 |
| `ports` | 差し替え対象の trait 境界（`SecretStore` / `ProcessProbe` / `LlmRouter` / `GitRunner` / `AgentSession`（再起動時のセッション再接続 F-37）、`SignalPort`（#136。driving adapter がフックシグナルを投入する境界。`submit(AgentSignal) -> Future<SignalAck, SignalError>` の RPITIT、Engine 非依存で hook_uds を隔離）、`FocusPort`/`FocusOutcome`（#155 F-94。`POST /focus` 制御要求の境界。シグナルと違い request-response で、`focused: bool` + 縮退理由を返す — 「フォーカスできない」は正常応答でエラーにしない）、**`Clock`（#174。壁時計取得の seam。`now_utc()` + 既定実装 `now_rfc3339()`（RFC 3339 フォーマットの単一実装）。StateDb の永続タイムスタンプ・worktree リテンション判定・シグナルタイムアウト sweep がこれ経由になり、時間依存テストを決定的に書ける。`logging/layer.rs` の tracing タイムスタンプは対象外）**、後続で `TaskSource` / `AgentIde`） | 各機能タスク / #136 / #155 / #174 |
| `paths` | XDG Base Directory 準拠のパス解決（`config`/`data`/`state`/`cache`/`runtime`、`totsuka` サフィックス、未設定時 `$HOME` フォールバック）。macOS でも XDG を明示採用 | #46 |
| `platform` | OS 依存実装の隔離。`platform::macos`（Keychain = keyring クレート）、`platform::unix`（`ProcessProbe` = `kill(pid,0)`）、`platform::fallback`（非 macOS の未サポート SecretStore）、**`platform::onepassword`（#156、[ADR-0006](/decisions/adr-0006-onepassword-secret-backend.md)。`op read --no-newline` シェルアウトで `op://<vault>/<item>/<field>` を解決。`#[cfg]` ゲートなし = 全ターゲットで動き、非 macOS 初の実働バックエンド。stdout（平文）はログ・エラーに出さず stderr のみで分類 — 未導入 `BackendUnavailable`（brew install 1password-cli）/ item 不在 `NotFound` / 未サインイン「`op signin` を実行」。runner seam で実 `op` 非依存テスト）**。**`platform::command`（#444、[ADR-0044](/decisions/adr-0044-cmd-secret-scheme.md)。`/bin/sh -c` シェルアウトで `cmd:<command>` を解決し stdout を秘密値に使う。末尾改行は除去・空出力は起動時エラー・stderr 先頭行のみ引用（stdout 不引用は op と同一の §5.2 不変条件）。runner seam + 実 `/bin/sh` を通すテスト 1 本）**。`PlatformSecretStore` は **合成ストア**: `SecretRef` の variant（enum 化 #156）で `Keychain` → Keychain/フォールバック、`OnePassword` → `OnePasswordCli`、`Command` → `CommandSecretStore`（#444）へ振り分ける | #46 / #156 / #444 |
| `config` | 設定ロードと検証。`schema`（`config.toml` パース、F-60/61/64。**#415 で `WorkflowConfig.initial_prompt`** — `[[workflows]].prompts`（#314）とは別レイヤで、置換ではなく連結・検証なし・送り先も違う）（**#554 で `plugin_settings` = トップレベルの未知テーブルを丸ごと保持する catch-all を追加し、`raw.rs` / `PluginRawConfig` を置き換えた**。`[[projects]]` もここ）、`resolve`（`${ENV}`/`keychain:`/`op://`/`cmd:` シークレット解決・`~`/`${ENV}` パス展開、F-62/65。**#217 で CLI から移動**: `secret_resolver(env)` = `PlatformSecretStore` + env スナップショットの合成コンストラクタ、`resolve_strings` = 任意 JSON の文字列リーフへの再帰シークレット解決 — store ジェネリックでテストはフェイク注入可）、`env_overrides`（**#208、[ADR-0009](/decisions/adr-0009-env-override-whitelist.md)**: F-66 第 2 層 `TOTSUKA_*` の適用。`apply_env_overrides(&mut RootConfig, env)` が**明示ホワイトリスト**（env 名 → 型付きフィールドの静的表、14 キー）に沿って `config.toml` パース直後の値を上書きし、警告リスト（未知キー・空値）を返す。不正値と「`[llm]` 不在での `TOTSUKA_LLM_*`」は `ConfigError::EnvOverride` で起動エラー、注入系の予約名（`TOTSUKA_JOB_ID` / `TOTSUKA_HOOK_*` / `TOTSUKA_PROMPT_CONTEXT`）は警告なしで無視。優先順位はマージ器ではなく**適用順**で実現する（CLI フラグは呼び出し側でこの後に適用）。旧 `layered`（4 層のフラット文字列 `HashMap` + `ConfigResolver`）は型強制もネストキー規約も持たず `RootConfig` へ接続できないまま**どこからも構築されていなかった**ため削除）、`validate`（静的検証 F-63/58 ＋ ワークフロー検証 F-81/82/83 を統合する `validate()` エントリ、Error/Warning の Finding を返す）、`edit`（`toml_edit` による**生テキスト in / 生テキスト out** の編集。コメント・順序・空白を保持するので結果は手編集と同一で、`config.toml` が単一の真実であり続ける（F-56/57）。`RootConfig` を経由した「ロード → 変更 → 再直列化」にしないのは、コメントが消えて順序が変わるうえ、`TOTSUKA_*` の env レイヤが畳み込まれてオーバーライドをユーザーが書いたかのようにファイルへ書き戻してしまうため。**#347 で `set_plugin_enabled` 以外に `upsert_repository` / `upsert_workflow` / `set_llm` / `set_tool` / `set_default_tool` / `set_hooks_auth_token_ref` を追加**（`totsuka setup` の部品）。配列表は**位置ではなく `name` で同定**して upsert するので、同じ draft の再適用がバイト単位で no-op になる（追記してしまうと `validate` の `DuplicateRepo`/`DuplicateWorkflow` でファイルごと拒否される）。書き込みは `set_value` を通し、**値が変わらないなら何も触らず、変わる場合も行の decor（末尾コメント）を引き継ぐ** — 素の `table[k] = value(x)` は `Item` ごと差し替えるため `name = "x"  # ours` の `# ours` が消え、このモジュールの存在理由そのものを損なう。`trigger` / `on_success` / `on_failure` は**インラインテーブルのフラグメント文字列**で受ける（スキーマ自身が無型の `toml::Table` として持っており、Rust の型を被せると存在しない構造を発明することになる）。不正なフラグメントは文書に触れる前に `BadFragment` で弾く） | #47 / #52 / #54 / #347 |
| `plugins` | プラグインの on-disk ストア（`store`）。install（prepare→confirm→commit の2段、SHA-256 表示 §5.4、manifest/protocol 互換検証 F-54）・uninstall・list。install=バイナリの存在、enabled=設定の宣言を分離（F-56）。**#345: `prepare_install_from(manifest_path, binary_dir)`** — manifest とバイナリを別々の場所から取れる parts API で、`prepare_install(dir)` はその薄いラッパ。バイナリは常に `binary_dir/<manifest の name>` なので [ADR-0027](/decisions/adr-0027-plugin-artifact-naming.md) の命名不変条件は弱まらない。あわせて `InstallPlan` が `manifest_path` を明示フィールドで持つようにし、`commit_install` が `source` ディレクトリから `plugin.toml` を再導出する暗黙結合を解いた（Cargo チェックアウトから staging ディレクトリを組まずに install する #346 の前提）。**`spec`（#217 で CLI から移動）**: enabled プラグイン 1 つの起動スペック組み立て `plugin_spec(store, cfg, name, env) -> PluginSpec`（F-58/65）— store の manifest/バイナリパス + `plugin_init_config`（**config.toml の `[<name>]` テーブル**を JSON 化 → `resolve_strings` でシークレット解決。欠落は空オブジェクト。#554 で `plugins/{name}.toml` の読み込みごと置き換わり、`config/raw.rs` と `PluginRawConfig` は削除された）+ config.toml 素材（`workflow_infos` = その名前を `source` または `agent` として名指す `[[workflows]]` を定義順に → `WorkflowInfo`（`trigger` はソースにだけ、`options` は両方に）#554、`repo_infos` = `[[repositories]]`→`RepoInfo`（パス展開 best effort、`project` の紐付き）#109/#554、task_source 向けに `project_infos` = `source` が一致する `[[projects]]`→`ProjectInfo` #554、`llm_info` = `[llm]`→`LlmInfo`（`api_key_ref` 解決、解決不能は None の best effort — doctor の `llm` チェックと run 自身のルータ構築が問題を表面化する）#119、`poll_interval_secs` 0.1.6）を合成。エラーは `SpecError`（未インストール/store/IO/parse/シークレット解決失敗、「原因 + 次のアクション」規約）。`run` / `doctor` / `config validate` の 3 コマンドが共用（doctor と validate は解決済み `init_config` を再利用して Keychain/Touch-ID の二重プロンプトを回避） **#542 で `claims` を追加、#554 で縮小**: `ClaimRegistry` が各 task_source の `initialize` 応答（`claimed_repos`）を統合し、リポジトリ → 起票先の**散文**を引けるようにする。**どのプラグインが担当かを決めるのはもう core の設定である**（`[[repositories]].project` → `[[projects]].source`、[ADR-0058](/decisions/adr-0058-config-ownership-boundary.md)）。プラグインに聞くのは散文だけで、それは agent のプロンプト向けなので core には組み立てられない。**跨ぎの重複検出（`ClaimConflict`）は削除した** — 1 リポジトリが 1 project を、1 project が 1 source を名指すので、2 プラグインが同じリポジトリを主張する状態が**書けない**。2 つ目の claim は最初のものを上書きしない（渡していないリポジトリを claim したプラグインが、タスクの行き先を動かさないため）。**`plugins/options.rs`（#554）**: `check_workflow_options` が `[[workflows]]` のプラグイン定義キーを、その workflow の `source` と `agent` が申告した `claimed_options` に照らす。ちょうど 1 つの引き取り手を要求し、0 は `Unclaimed`（タイポ）、2 は `Ambiguous`。**答えなかったプラグインが居る workflow は判定しない** — 起動失敗と「何も引き取らない」は wire 上で区別できず、前者を後者と読むと正しい config に「未知のキー」が並ぶ | #52 / #217 / #542 / #554 |
| `repo_select` | リポジトリ自動選択（F-10〜15）。repo_hint 解決を最優先、未解決なら LLM 分類（候補=概要＋README先頭N行）。confidence 低/未知repo（1回リトライ後）は pending、API 恒久失敗は failed。`reason` は --dry-run 用。`ReadmeCache`（README hash キャッシュ、F-15）。LLM は `ports::LlmRouter`（OpenAI 互換）越し、reqwest 実装は `adapters::llm::OpenAiRouter`（指数バックオフ §5.3）。**#267: `OpenAiRouter::probe_auth`**（`LlmRouter` の本経路を通さない専用メソッド。json_schema なし・リトライなし・`max_tokens: 1`・本文破棄で「鍵が受理されるか」だけを訊く。`totsuka doctor --online` 専用、[ADR-0016](/decisions/adr-0016-doctor-online-probe.md)）と **`LlmError::is_auth_failure()`**（`Status { 401 \| 403 }` のみ true。`is_retryable()` と直交し、認証拒否は終端） | #56 / #267 |
| `scheduler` | 並列実行制御（F-40〜45）。3 階層スロット（global/repo/agent）を全て満たすと dispatch 可、`counts_toward_slot` は dispatched/running/verifying/publishing を計上（waiting_input/escalated は解放。verifying は出力未確定のため Publishing 相当で保持、escalated は人間待ちのため WaitingInput 相当で解放 #133）。DB から `rebuild` で再構築。`PriorityQueue`（priority 降順→FIFO）、`plan_dispatch`（優先度順にスロット確保） | #55 / #133 |
| `agent_tools` | **profile が要求する外部ツールの可用性判定（#399、[ADR-0033](/decisions/adr-0033-workflow-profile.md) D9）**。`required(profile)` は `implement` の `gh` だけを返す（`triage` / `design` の書き込み先は source 依存で判別できず、推測して誤ると動いたはずのタスクを止めるため）。`available` はバイナリの PATH 解決と `gh` の認証ファイルの存在だけを見るローカル判定で、`gh auth status` は実行しない（dispatch 経路にネットワークを持ち込まない）。`ToolCache` が 5 分 TTL で答えをキャッシュし、`now: Instant` を引数に取るので expiry を sleep なしでテストできる。**#407**: `BLOCKED_NOTE`（[ADR-0037](/decisions/adr-0037-task-notes-in-the-event-log.md) のノート kind）と `blocked_reason(&[&str])`（remedy と偽陰性の但し書きを含む一文）を持ち、**通知（#399）と `totsuka status`（#407）が同じ関数を使う**。`AgentTool::parse` は記録されたツール名を読み戻す口で、未知の名前は `None`（このバイナリが知らないツールを記録したノートを説明できる必要がある） | #399 / #407 |
| `worktree` | git worktree ライフサイクル（F-20〜25/85）。作成（fetch→origin/{default} を commit 解決して分岐→worktree add、並列安全）、掃除（immediate/retention/manual、未コミットは skip。**#210（[ADR-0010](/decisions/adr-0010-worktree-cleanup-pane-release.md)）で判定と実行を分離**: `decide_cleanup`（dirty チェック + **detached-with-commits チェック** + policy 判定 → `CleanupDecision::{Remove,Retain,Dirty}`。後者は detached 作成が新設したデータ損失経路への蓋 — detached HEAD 上のコミットは `git status --porcelain` が空なので F-23 の dirty ガードをすり抜け、`worktree remove` がその唯一の到達性を持ち去る。plan モードは git を実行できずコミットが 0 なので影響を受けない）と `remove`（削除の実行。ブランチ引数は `Option` — worktree がブランチに載っていないことはありえ、`None` は「消す対象のブランチが無い」であって「掃除をやめる」ではない。ブランチ削除には **base_commit 由来判定**（`merge-base --is-ancestor <base_commit> <branch>`）が前置される: 「origin のどこにも無いコミットが無い」だけでは*誰の*ブランチかを何も言っておらず、名前が運用者と同じ名前空間に入った以上それでは足りない。古い既定ブランチから切られた人間のブランチはこの worktree の起点を含まないので保護される。`base_commit` が無い（v8 以前の行）なら削除しない。冒頭で dirty を**再チェック**し、判定〜実行の間 — pane close の窓 — に dirty 化していれば `DirtySkipped` で中止 = TOCTOU 対策。データ損失 > pane 喪失）。既存 `cleanup()` は両者を呼ぶ薄いラッパとして互換維持）、孤児検出。ブランチ/配置テンプレート描画とサニタイズ — `render_worktree_name` は `render_legalized` で task_id 由来の git ref 禁止文字（`:` 空白 `~^?*[\` 制御文字、`..`/`.lock`/`@{`、先頭 `-` の option 注入）を `-` へ合法化する（task_id はソース定義で、Slack は `{channel}:{ts}`。git 制約は git 境界で一括吸収、#108）。**ブランチはエージェントが命名する**（[ADR-0026](/decisions/adr-0026-agent-owned-branch-and-push.md)） — `create` は `worktree add --detach <path> <base_commit>` で detached のまま引き渡し、Orchestrator は名前を一切生成しない（リポジトリ内に書かれた命名規約に、生成した名前は従えない）。名前は `head_branch`（`rev-parse --abbrev-ref HEAD`）で読み戻す。**push・PR 作成も行わない（F-86 撤回）** — `push_branch` / `has_commits_to_publish` と `run::output`（`PrCreator` / `GhPrCreator`）は削除した。**worktree のディレクトリ名はブランチ由来をやめ、`DEFAULT_WORKTREE_NAME_TEMPLATE = "{source}-{task_id}"` から `render_worktree_name` が作る**（git ref 規則は 1 パスコンポーネントに必要な条件の真部分集合として流用し、ref 規則が意図的に残す `/` は `sanitize_branch_for_path` が潰す）。配置テンプレートのプレースホルダも `{branch}` → `{worktree_name}` に置き換わった — worktree を作る時点でブランチ名は存在しないので、原理的に描画できない。旧 `{branch}` は `config::validate` が専用のエラーで拒否する（「未知のプレースホルダ」ではタイポに読めるため）。既存の worktree は移行不要 — 掃除・孤児検出・`doctor`・ディスパッチ時の再利用ガードはすべて state.db に記録済みの path を読み、テンプレートを引き直さない。**配置の既定値は `default_location_template(&Paths)`** が `Paths::state_dir()`（= `$XDG_STATE_HOME/totsuka`、未設定なら `$HOME/.local/state/totsuka`）から**解決済みパスとして**組み立てる。旧実装は `"${XDG_STATE_HOME}/totsuka/worktrees/..."` というテンプレート文字列定数で、`expand_env` が未設定変数をエラーにするため `XDG_STATE_HOME` 非設定機（macOS 既定）では**全ディスパッチが worktree 作成で失敗**していた。`[hooks].socket_path`/`spool_dir` と同じ「ユーザー指定なら `expand_path`、既定値なら `Paths`」規約に統一したもので、`XDG_STATE_HOME` 設定時の解決先は従来と同一（移行不要）。ユーザーが `[worktree].location` を書く場合の `${ENV}` 展開は従来どおり。**#254 で `create` を再作成対応**。ブランチの由来を `branch_source` が 3 段で決める: ①ローカルブランチが残っていれば `-b` なしで checkout（`base_commit` へリセットしない — `branch -d` が拒否した未マージコミットを復旧経路で破壊しないため）②ローカルが無く `origin/{branch}` が残っていればそのコミットから再作成（`push_branch` が `push -u` で upstream を張るため、**publish 済みブランチは掃除で削除される**（origin に全コミットがあるため。#266 以前は `branch -d` の upstream ルールがこれを担っていた） = 掃除後にコミットがリモートにしか無い状態が `output = pull_request` では常態。ここで `origin/{default}` から切ると publish 済みの作業が孤立し次の `push -u` が non-fast-forward で失敗する）③どちらも無ければ **detached**（記録名は既に何も指しておらず、空ブランチを復活させて継続性を偽装しないため。エージェントが名前を付け直す）。`existing_branch` が最初から無い通常の初回ディスパッチも detached。remote 由来でもコミットに解決してから渡す（remote-tracking ref から切ると upstream 設定が `.git/config` を書き並列作成で競合するため）。加えて 2 種の復旧を行う: ディレクトリだけ手動削除された stale 登録は `worktree prune` 後 1 回だけ再試行（`is_stale_registration` は git の "missing but already registered" のみを判定し、**生きている** worktree との衝突は従来どおりエラー = 誤検知の害 > 見逃しの害）、`worktree add -b` は**非アトミック**でブランチだけ作って失敗しうる（transient retry がコマンド全体を再実行するため、並列作成が `commondir` レースに負けると 2 回目が自分の 1 回目の作ったブランチに衝突する）ので、失敗後にブランチが出現していれば `-b` なしで 1 回だけ再試行する。**#266 で `remove` のブランチ削除を作り直した**: 旧実装の `git branch -d` は「マージ済み」をローカル `HEAD` 基準で判定するが、worktree のブランチは `origin/{default}` から切られるため、**ローカルの既定ブランチが origin より遅れているという常態**で必ず拒否され、実機で `agent/*` が 5 本溜まっていた（結果を `let _ =` で捨てていたためログにも出ていなかった）。`delete_branch_if_published` は代わりに `rev-list --count <branch> --not --remotes=origin` で「**origin のどこにも無いコミットがあるか**」だけを見る — 0 なら `-D`（失うものが無い）、1 以上なら保持して理由をログに出す。この形にしたのは、issue 提案の `merge-base --is-ancestor origin/{default}` だと **push 済みで PR が未マージのブランチ**（`-d` が upstream ルールで正しく削除できていたケース）を保持する側に倒してしまうため。数えられない場合（壊れた ref 等）も保持する — 安全だと証明できないことは破壊してよい理由にならない | #53 / #266 |
| `logging` | 構造化ログと機密マスキング。`redact`（フィールド denylist＋値パターン）、`layer`（redact 済み JSON Lines / 人間可読を出力する tracing レイヤ）、`rotation`（日次ログの世代保持）。`init`（ファイル + stderr、`run` 用）に加え `init_stderr`（stderr のみ・ファイル生成なし。`run` 以外のコマンドの `--debug` 用、#176）。規約は [ログ規約](/development/logging-conventions.md)。**#297: human 形式（stderr）だけフィールド値と message を `terminal::safe()` に通す** — `run` が `title = %task.title` を出しており、`--debug` は全コマンドでこの層を通るため、外部由来テキストが端末へ直接届いていた。redact（誰が読んでよいか。両形式）とエスケープ（画面に何ができるか。human のみ）は**別段**にしている — JSON 形式は `jq` が読むので `serde_json` が書いた値のままでなければならない | #49 / #297 |
| `prompts` | **claude / codex / opencode に差し込むプロンプト文の置き場（#313 / #314、エピック [#311](https://github.com/tomoya-k31/totsuka/issues/311)）**。組み込みデフォルトは Rust の文字列リテラルではなく埋め込み `src/prompts/defaults.toml`（`include_str!` + `LazyLock`）に置き、文言調整をコード変更ではなくデータファイルの編集にする。キーは `marker_self_report`（全ディスパッチに `TOTSUKA_PROMPT_CONTEXT` 経由で注入する完了自己申告指示。invisible injection 非対応ツールでは可視 `extra_context`。**design / implement profile では `marker_self_report_confirm` が既定** — 完了と思ったら NEEDS_INPUT で人間に確認を求め、pane 上の明示承認後にのみ COMPLETED を出す規約、#440）/ `branch_convention`（detached で引き渡す worktree でブランチを切らせる指示。**plan モードでは注入しない** — plan ペインは git を実行できず、claude では無人ペインが答えられない承認プロンプトを誘発してタイムアウトになるため、出すこと自体が害。既にブランチ上のタスクにも出さない）/ `verification_rubric`（profile 既定の差し替え変種が 2 つ: `verification_rubric_artifact_url` = triage の成果物 URL 検収 #398、`verification_rubric_human_approval` = design / implement の「人間が会話上で明示承認済みか」検収 #440） / `verification_background_exemption` / **`verification_nonclaim_exemption`**（#389） / `verification_marker_convention` / `verification_prompt`（前 4 者の組み立て方自体もテンプレート）。**`verification_*` は命令ではなく条件**である — Claude Code は `prompt` 型フックの本文を「ユーザ提供の条件が満たされているか」としてモデルに判定させ、`{ok, reason}` を返させる（本体 2.1.224 で確認）。`ok: false` がブロックで `reason` がエージェントへ差し戻る。**モデルはブロックを制御していないので「許可してください」と書いても効かない** — #389 は最初その形で出荷し、実機でジャッジが当該文言を逐語引用しながら 8 回連続 `ok: false` を返した。条件文に組み替えた版は実機で `NEEDS_INPUT` 停止のブロック 0 回を確認済み。**#316: `opencode_plan_agent`**（opencode plan エージェントの散文本体。グローバル専用 — ディスク上の 1 枚を全セッションが共有するため）を追加し、`hooks::opencode` の `ASSETS` を `STATIC_ASSETS`（JS プラグイン = コード実行面、固定）と `rendered_assets()` に分割。**YAML frontmatter（`permission` の deny マップ）は `PLAN_AGENT_FRONTMATTER` として Rust 側固定**で、散文に見えるキーから `bash: allow` を注入できると権限昇格になるため設定不可。**散文自体も #465 で組み込みへ戻った**ので、それを設定できた間だけ存在した「散文が `---` 始まりなら検証エラー」の検査も一緒に消えた（弾く対象だった運用者の値が無い）。`---` を含まない不変条件は `plan_agent_renders_its_exact_bytes` が全バイト一致で固定する。`opencode::verify_assets(config_dir)` は毎回再レンダリングした期待値と比較するので、ディスク改変の検知は従来どおり。`{marker_*}` は `domain::signal` のワイヤ定数へ解決され、**マーカー自体は設定不可**（`on-stop.sh` と `totsuka-opencode.js` がリテラルを parse し、[ADR-0020](/decisions/adr-0020-status-marker-stays.md) が 3 ツール共通の唯一の完了信号と定めている）。展開は `template` 経由の**段階的シングルパス** — 葉（マーカー置換）→ 組み立て、の 2 段で各段シングルなので、rubric 内に書かれたリテラル `{marker_convention}` は挿入されるだけで展開されない。**プロンプトはモデルに何を伝えるかだけを変え、何が動くかは変えない**（スクリプト・argv・permission ブロックには触れない）。**#314 の `[prompts]` / `[[workflows]].prompts` による上書きは #465 で削除**（[ADR-0023 の Amendment](/decisions/adr-0023-configurable-prompt-surface.md)）。上書き面は `[[workflows]].rubric` 1 キーだけになり、`Prompts::resolve_for(wf)` が解決する梯子は **3 段**（`workflow.rubric` > profile 既定 > 組み込み）。消したのは「グローバルな面」で、キーを 1 つ書くだけで後から入った profile の検収（triage の URL 検収 / design・implement の人間承認検収）が黙って無効化される梯子がそこにあった — **症状が「検収が緩くなる」方向へ倒れる**のが決め手。`PromptSet::from_config` は全ワークフロー分をまとめて `EngineSettings.prompts` に載せる（`tool` のレジストリと同じ流儀で、ディスパッチは config を読み直さない）。`PromptSet::for_workflow` は未知名で組み込みへフォールバックし panic しない（タスクはワークフローより長生きしうる）。削除したキーは**パースはされ検証で落ちる** — 素の `unknown field` ではなく、キーごとに何になったかを名指しする `RemovedPromptKey`。`ALLOWED_PLACEHOLDERS` は残り、検査対象が「運用者が書いた値」から**`defaults.toml` 自身**へ移った（全ビルドに載るアセットのタイポが従来は無検査だった） | #313 / #314 / #316 |
| `template` | 設定に書かれたテンプレートの `{placeholder}` 展開（#312）。`render(template, vars)` は**シングルパス**（置換した値を再走査しないのでテンプレート注入が成立しない）で、未知キーと不均衡な `{` はそのまま出力する fail-soft。`scan(template, skip_dollar)` は参照されているプレースホルダを列挙し、`config::validate` がタイポを事前に弾くのに使う（`skip_dollar` は `${ENV}` を飛ばすかどうかで、worktree テンプレートのみ true）。**#328: `scan` は `ScanMode` を取る** — 置換エンジンが 2 つあり、何をプレースホルダと見なすかが違うため。`Rendered`（`render` 経由 = プロンプト）は名前を識別子（`[A-Za-z_][A-Za-z0-9_]*`）に限定する。波括弧は中身としても現れる（プロンプトが `{"ok": true}` のような JSON 出力形をモデルに示す等）ためで、`render` は元々そういう中身を素通ししていたのに `scan` だけが未知プレースホルダとして報告し、呼び出し側は全て Error 化するので**描画結果が正しい config が起動を拒否されていた**。`Replaced`（`worktree` の `str::replace` 連鎖）は**全ブレース範囲**を検査し `${ENV}` を飛ばす — `.replace("{worktree_name}", …)` は `{a{worktree_name}` のような大きい範囲の中の `{worktree_name}` も置換するので、ここで絞ると**置換されるのに検査されない**プレースホルダができるうえ、`{repo-name}` のタイポがリテラル名のディレクトリを黙って作る。`render` 側を狭めて揃えてはいけない（未知キー素通しは意図的な fail-soft）。識別子限定の裏返しとして識別子でない綴りのタイポは検出できないので、マーカーは**3 つすべてが組み立て後の出力に現れること**を直接検査する（`missing_markers`。「どれか 1 つでもあれば良い」では 2 つ残ったタイポを見逃す）。波括弧の入れ子（`{ {rubric}` = 中の本物を飲み込む形）は `has_swallowed_brace` が警告する。`config::validate::check_worktree_placeholders` が使う。**`worktree` の `render_location`/`render_worktree_name` は意図的にここを通さない** — `.replace()` 連鎖でマルチパス（置換値を再走査する）かつ git ref 正規化と結合しており、統一すると worktree 側の契約が変わる | #312 / #328 |
| `terminal` | **外部由来テキストを端末へ出す直前の無害化（#280 / #297）**。`safe(&str) -> Cow<str>` の唯一の実装。制御文字（C0/C1/DEL）と bidi override/isolate を**除去ではなく可視のエスケープ**へ置き換える。core に置いてあるのは印字側が 2 クレートに跨がるため（[orchestrator-cli](/components/orchestrator-cli.md) の human 出力と、core 自身の `logging::layer` の stderr 出力）。CLI の `common::safe` は本モジュールの re-export。`--json` / JSON ログには**通さない**（二重エスケープ）。→ [端末出力の信頼境界](/security/terminal-output-sanitization.md) | #297 |
| `recovery` | 再起動回復（F-37/44、§5.3）。`recover` が回復対象状態（`RECOVERABLE_STATES` = dispatched/running/waiting_input/verifying/escalated/publishing）のタスクを `session/attach` で再接続し、エージェント状態にステートマシンを前方同期（`resume_plan`）／attach 失敗は「継続確認待ち」（`RecoveryReport`、**自動 failed にしない**）。**human-gated 安全化（#133）**: `verifying`/`escalated` はエージェント状態に関わらず常に Confirm（検収・エスカレーション解消を再起動で自動スキップしない）、`(agent Done, waiting_input)` も自動 Publishing せず Confirm（human 検収相当の検収スキップ穴を封鎖）。`retry_plan`（worktree＋セッション再利用 F-44）・`active_slot_claims`（`SlotManager::rebuild` 用 (repo,plugin)）。詳細 → [state.db スキーマ](/data/state-db.md) | #57 / #133 |
| `tool` | AI ツール抽象（#196、[ADR-0014](/decisions/adr-0014-tool-abstraction.md)）。`ToolKind`（claude / codex / opencode。`has_adapter()` = 完了検知アダプタの有無 — claude / codex / opencode すべて ✓（#196 Phase 1〜3 完了。アダプタ無し kind の拒否経路は将来の kind 追加に備えて残置））、`ToolCapabilities`（不可視注入・marker block・prompt 検証・resume・plan・heartbeat・session id 捕捉 — 縮退制御の静的表。codex は 2026-07-24 実機スパイク（codex-cli 0.145.0）で確定: prompt 検証 ✗・heartbeat ✗ 以外 ✓。opencode は同日スパイク（1.14.39）で確定: 不可視注入 ✗・marker block ✗・prompt 検証 ✗・heartbeat ✗、resume/plan/session id 捕捉 ✓）、`ToolProfile`（`[tools.<name>]` の解決結果。組み込み `claude` / `codex` / `opencode` + `registry_from_config` で組み込みを `[tools]` エントリが上書きするレジストリを構成）、`launch_spec(LaunchInputs) -> Option<ToolLaunchSpec>`（kind ごとの argv 組立。claude: base → plan なら `--permission-mode plan`（上書き可） → `--settings <path>` → `--resume <id>`。**#410 / #409 以降、plan 引数は条件付き**: `LaunchInputs.profile`（新設）が **read-only profile**（answer / triage / design）で**かつ** `settings_path` がある（= deny が実際に届く）ときだけ落とす（[ADR-0036](/decisions/adr-0036-read-only-violation-fails-the-task.md) D2 が ADR-0035 の「Bash も deny する profile だけ」を広げた — plan モードの強制力は未実測なのに無人 pane を 14 分止める実測があるため）。両方要るのは、deny が `--settings` 経由でしか届かず、そのパスはフック対応 agent にしか解決されないため（片方だけで判定すると deny も plan も無い起動になる）。明示 `plan_args` は落とさない。[ADR-0035](/decisions/adr-0035-answer-profile-shell-removal.md) / [ADR-0036](/decisions/adr-0036-read-only-violation-fails-the-task.md)。codex（Phase 2）: base → resume なら `resume <id>` **サブコマンド** → implement 既定 `--sandbox workspace-write --ask-for-approval never` / plan 既定 `--sandbox read-only --ask-for-approval never`（plan permission mode 不在の縮退、実機スパイク [V3]）。`settings_path` は不使用 — フックはグローバル登録 + env ゲート（`hooks::codex`）。opencode（Phase 3）: base → plan なら `--agent totsuka-plan --auto`（全 deny の plan エージェント） → implement 既定 `--auto` → resume なら `-s <id>`。`settings_path` 不使用（完了検知はグローバル配置の JS プラグイン、`hooks::opencode`））。**`--ask-for-approval never` / `--auto` / claude の `permissions.defaultMode = "auto"` は同じ 1 つの決定の 3 通りの綴りで、pane には承認を答える人が居ないという事実に対応する（[ADR-0041](/decisions/adr-0041-unattended-permission-mode.md)）。境界は緩めない — claude の deny は全 permission mode で効き、codex の `--sandbox` は別フラグ、opencode の `--auto` は explicitly denied を除いて自動承認する。** ツール解決の優先順位は `resolve_tool_name`: workflow ピン > repo 既定 > `default_tool` > 組み込み `claude` | #196 |
| `hooks` | **フックスクリプト + workflow 別 settings のレンダリング（#131 H-01/H-03, #137。#178 で [orchestrator-cli](/components/orchestrator-cli.md) から移動）** — 描画側（本モジュール）と受信側（`run::hooks`）・マーカー定数（`domain::signal`）がこれで単一クレート内に閉じ、フック仕様の変更が 2 クレートにまたがらない。静的スクリプト 7 本（`hook-common.sh` / `on-stop.sh` / `on-notification.sh` / `on-ask-user-question.sh`（#487、design / implement の settings にだけ配線される AskUserQuestion の PreToolUse リレー。stdout は常に空 — PreToolUse の stdout は許可判定になるため） / `on-session-start.sh` / `on-session-end.sh` / `on-user-prompt-submit.sh`）を `include_str!` でバイナリ同梱し、`install` が `run` / `doctor` 起動時に `$XDG_DATA_HOME/totsuka/hooks/` へ **0700・内容ハッシュ（SHA-256）冪等**で書き出す（一致なら書き換えず mtime も保存、バージョンアップ時のみリフレッシュ。モードのみのドリフトは内容一致でも再適用して修復）。あわせて **workflow 別 `orchestrator-<workflow>.json`** を **0600** でレンダリング（`settings_path` がパスの単一定義）: `Stop` には常に `on-stop.sh`（command 型）、`verification = "llm"` の workflow のみ `prompt` 型フック（`workflow.rubric` か既定 rubric + 中間停止免除文 + マーカー規約）を追加（D-01）。**#313 以降、これら 3 つの文面とその組み立て方は `prompts` の `defaults.toml` に置かれ、本モジュールは `Prompts::verification_prompt()` を呼ぶだけになった**（`workflow.rubric` は rubric の葉だけを差し替える）。文中の `{marker_*}` は `domain::signal` の共有定数 `MARKER_COMPLETED` / `MARKER_NEEDS_INPUT` / `MARKER_FAILED` へ解決され、ディスパッチ側の自己申告指示も同じ定数を使うためワイヤ規約の Rust 側定義は一箇所（bash 側 `on-stop.sh` のパースは言語跨ぎのため手動同期 + 受信側の same-shape テストで担保）。免除文は `on-stop.sh` の R-02（`background_tasks` 非空 = ハートビート）と対で、バックグラウンドタスク実行中の中間 Stop を判定 LLM が検証・ブロックしないよう機械的に付加する。`Notification` / `SessionStart` / `SessionEnd` / `UserPromptSubmit` は各 command 型フック（`UserPromptSubmit` は全 workflow 共通配線 — スクリプトが env 未設定時に no-op するため常時登録して安全）。**ジョブ固有値はファイルに書かない**: job_id / エンドポイント / トークン / スプールディレクトリ / プロンプトコンテキストは env（`TOTSUKA_JOB_ID` / `TOTSUKA_HOOK_ENDPOINT` / `TOTSUKA_HOOK_TOKEN` / `TOTSUKA_HOOK_SPOOL_DIR` / `TOTSUKA_PROMPT_CONTEXT`）で [agent-ide-herdr](/components/agent-ide-herdr.md) が注入（`HookLaunchSpec`）し、同一の `--settings` パスが `claude --resume` を跨いで再利用できる（H-03）。`verify_assets` は**書き込みなし**の検査（存在・0700/0600・内容ハッシュ一致 → `AssetIssue` の列挙）で、doctor の N-02 タンパー検出に使う。**#196 Phase 2: スクリプトのツール非依存化 + `hooks::codex`（codex 登録管理）** — codex はフックを `$CODEX_HOME/hooks.json` へ**グローバル登録**するため（`--settings` 相当なし）、全スクリプトに `TOTSUKA_JOB_ID` 早期ゲート（未設定 = 個人セッション → 即 exit 0。特に `on-stop.sh` が個人セッションを block しない保証）を追加し、`on-stop.sh` は `prompt_id // turn_id`（codex のターンキー）フォールバック、`on-notification.sh` は codex `PermissionRequest`（Notification イベント不在の代替）を `message = "permission_prompt: <tool_name>"` へ合成して同一ワイヤ形で送出（stdout は常に空 = 承認判断へ不干渉）。`hooks::codex` は totsuka 管理エントリ（Stop/SessionStart/SessionEnd/UserPromptSubmit/PermissionRequest、SessionEnd は codex の 3s クランプに合わせ timeout 3）をコマンドパス基準の構造判定で同期: 非管理エントリは**既存 index のまま**意味的に保全（codex の trust レコードは index キーのため in-place 置換）、パース不能 JSON は上書き拒否、`[hooks.state]` の trust 存在プローブ `untrusted_events` を doctor へ提供（未 trust エントリは codex が**サイレントスキップ**するため警告必須）。同期は config が codex-kind ツールを参照する場合のみ（claude-only 構成は `$CODEX_HOME` に触れない）。**`hooks::opencode`（#196 Phase 3）**: 完了検知 JS プラグイン `totsuka-opencode.js`（session.status(idle)/session.idle 両対応 → `client.session.messages` で最終 assistant メッセージ取得 → マーカー解析（on-stop.sh と同じ 1〜2 カッコ許容・最後勝ち）→ Bun fetch `unix:` で UDS POST。prompt_id = メッセージ id（不在時は任意ロール最終 id へフォールバック — 空文字だと同 status の Stop が冪等キーに畳まれるため）。session.created → SessionStart、session.error → FAILED（per-occurrence id）。POST 失敗は spool。env（TOTSUKA_HOOK_ENDPOINT/JOB_ID）無しではフック未登録 = 個人セッション保護）と plan エージェント `totsuka-plan.md`（permission edit/bash/task **全 deny** — 部分 deny はサブエージェント委譲で貫通する実機発見）を `$XDG_CONFIG_HOME/opencode/{plugins,agents}/` へ SHA 冪等配置。trust 機構は無く配置のみで有効（参照ゲートは codex と共通の `config_references_kind`）。`on-stop.sh` は `set -uo pipefail`（`-e` は使わない = fail-open D-09）: ①`jq`/`curl` 欠落 → 生 JSON をスプールへ追記し exit 0 ②`background_tasks` 非空 → `heartbeat` を POST し exit 0（中間 Stop、R-02）③`last_assistant_message` の**最後の** `<<STATUS:...>>` マーカー（`reason="..."` 属性込み、D-12。**山カッコは単一/二重どちらも許容**）を抽出し該当 status を POST ④マーカー欠落 & `stop_hook_active != true` → `UNKNOWN` を POST の上、stdout に `{"decision":"block",...}`（是正方法つき、R-03）⑤マーカー欠落 & `stop_hook_active == true` → `UNKNOWN` の POST のみ（再 block しない）。POST は `hook-common.sh` の `post_event`（`curl --unix-socket` + Bearer + `--max-time 5 --retry 2`）、失敗時は NDJSON 1 行を `$TOTSUKA_HOOK_SPOOL_DIR/<epoch>-<pid>.jsonl` へ追記（E-07/H-11）。stdout は block JSON 以外を出さない（H-13）。`on-user-prompt-submit.sh` は**不可視プロンプトコンテキスト注入**（`UserPromptSubmit` フック）: env `TOTSUKA_PROMPT_CONTEXT` が設定されていれば `additionalContext` を 1 行だけ stdout へ出力し、env 未設定・空・`jq` 欠落時は無出力で exit 0（fail-open D-09、`on-stop.sh` の block がセーフティネット） | #137 / #178 |
| `run` | `run` メインループ（§5.1）。**#464 で 1 ファイル 4,153 行から責務ごとに分割**（`mod.rs` 777 = Engine 本体・`new`/`build`・`run`/`cycle`、`dispatch` = repo 選択とスロット制 dispatch と retry 再アタッチ、`ingest` = `task/submit` 取り込みと plugin 起点リクエスト、`finalize` = 出力ポリシーと書き戻しと worktree 掃除、`events` = `PluginEvent` 処理と状態機械の適用、`sweep` = ブランチ同期・read-only 検査・保持スイープ、`settings` = `RootConfig` の解釈、`support` = `self` を持たない純粋ヘルパ、`report` = `RunSummary` と #497 のプラグイン別 RPC 会計、**`supervise` = プラグインの死活監視と再起動（#495）+ 呼び出し統計の集約（#497）**）。**振る舞いは不変**（テスト 453 件が分割前と一致）で `Engine` のフィールドも 18 個すべて private のまま。ただし**メソッドには `pub(super)` が要る** — Rust の privacy は「定義モジュールとその子孫」なので、impl を子モジュールへ分けると親（`mod.rs`）からも兄弟（`hooks`）からも見えなくなる。フィールドの可視性とは別の話で、分割に伴い避けられない。複数の兄弟が使うヘルパは `mod.rs` / `support` に置く（兄弟へは `pub(super)` でも届かないため）。**#471 で `dispatch_one` を 513 → 236 行に分解**した: 決定は `resolve_dispatch_target`（**純粋関数**。`record` と `settings` と「その agent は起動しているか・何を宣言しているか」を答えるクロージャだけを取り、`DispatchTarget` か `DispatchRefusal` を返す）へ切り出し、副作用は `acquire_worktree` / `wire_hooks` / `record_dispatch_and_subscribe` の 3 メソッドへ。**#196 のツール解決優先順位と各 refusal の文面が、プラグイン起動も worktree 作成もなしに単体テストできるようになった**（従来は E2E でしか固定できなかった）。`DispatchRefusal` が 2 変種なのは、消えた workflow（スロット解放のみ・`dispatch_ready` が警告済み）と `fail_dispatch` 行きを分けるため — 設定を編集中の運用者のタスクを死んだ扱いにしない。`build_params` は据え置き（`SESSION_UNRESUMABLE` の再送で 2 回呼ぶクロージャで、外に出してもキャプチャに名前が付くだけ）。**#548: workflow 単位の publish / cleanup 上書き** — `finalize` は publish 時に `[[workflows]].publish` を wire の `delivery` へ翻訳し（未設定は**ワイヤに載せない** — 欠落が 0.5.2 以前のプラグインも読める形）、掃除は workflow の `cleanup` が `[worktree]` の mode 既定に勝つ。workflow が削除・改名済みで引けなければ mode 既定へ縮退し info log を 1 行出す（ADR-0057）。 **#542: `triage` の起票先注入** — workflow の profile が `triage` のとき、解決済みリポジトリを claim している source の `destination`（プラグインが自分の設定から組んだ散文）を **`task.instructions` の末尾へ連結**する。専用チャネルを作らないのは、instructions が既に「見えない経路（フックの prompt context）」と「見える経路（invisible injection の無いツール）」の両方を通っているからで、後から片方だけ通す穴を作らないため。`triage` に限るのは、成果物が**別の場所に置かれる item** である唯一の profile だから — `implement` は渡されたリポジトリの中で作業するので、触る筋合いのないボードの話は誤って行動しうるノイズになる。claim が無いリポジトリは何も足さない（トラッカー未設定の運用は正常で、0.5.1 より前のプラグインも同じ見え方になる）。`ClaimRegistry` は**dispatch のたびに組み直す** — 再起動（#495）は `Plugin` を作り直すので、起動時に 1 度取ったスナップショットは死んだプロセスの claim を語り続ける。 **#415: `prepend_initial_prompt`** — ワークフローの `initial_prompt` を**可視 `extra_context` の先頭**へ連結する。判定は `build_params` クロージャの**内側**で `resume.is_none()` を見るので、`SESSION_UNRESUMABLE` で resume を落として再ディスパッチする経路（実際に新規セッションになる）でも復活する。未設定なら `extra_context` はバイト同一 → [ADR-0038](/decisions/adr-0038-workflow-initial-prompt.md)。`Engine` が push（`task/submit`、防御的再マッチ含む）取り込み（F-73）→リポジトリ選択→スロット確保→worktree 作成→`task/dispatch`→`state/subscribe` の起動時サイクルと、`state/notification` 駆動の監視（waiting_input=スロット解放＋Notifier 配送 F-35/45、done=出力ポリシー実行→`on_success`/`on_failure` 書き戻し F-84→worktree 掃除 F-23/85、retry は worktree＋セッション再利用 F-44、プラグインクラッシュ=failed §5.3）。**#495: 死活監視を全 kind へ**（`supervise`）— 従来 `PluginEvent::Closed` は `agent_ide` の**通知ストリームの終端**にしか配線されておらず、`task_source` が死ぬとイベントが発生せず`--watch` が「永久にタスクを受け付けないプロセス」になっていた。検知を**子プロセスの終了そのもの**（`Liveness`）に紐づけ、指数バックオフ・最大 5 回 / 5 分のスライディング窓で再起動する。**agent は「在席タスクを畳む → 再起動」の順序が load-bearing**（逆にすると新プロセスのセッション ID が死んだプロセスのタスクに紐付く）。再起動後は**新インスタンスに対してストリームを張り直す**（`take_notifications` / `take_incoming_requests` は一回性なので、古い receiver を持ったままのタスクは永久に何も受け取らない）。予算を使い切ったら `NotifierEvent::Escalated` を`task_id`/`workflow` とも `None` で配送し（プラグインの死はどのタスクのものでもない）、`RunSummary.plugin_restarts` に計上する。`[plugins.{name}].restart = false` で再起動だけを無効化でき、検知は残る。**#497: プラグイン単位の観測性** — `RunSummary.plugins` に**プラグイン名 → メソッド名**の RPC 会計（呼び出し数・outcome 別内訳・直近 256 サンプルの p50/p95）とクラッシュ/再起動回数を出す。**再起動との噛み合わせが要点**で、再起動は `Plugin` を作り直すためカウンタがゼロに戻る — `install_restarted` が `insert`（＝古いインスタンスの drop）する**前**に `harvest_stats` で回収しないと、**最もクラッシュしたプラグインが最も呼び出しの少ないプラグインとして見える**。`PluginSet.specs` に spec が無いプラグイン（手組みのテスト等）は検知のみで再起動しない → [ADR-0051](/decisions/adr-0051-plugin-supervision.md)を統合するイベント駆動ループ。ワンショット（既定: 全 dispatch 済みタスクが終端/待機に達し、かつ直近イベントから静穏期間（`EngineSettings.one_shot_grace`、既定 2 秒・config 非露出、テストは `Duration::ZERO` で待たない — #281）が経過したら summary で終了 — **0.2.0（#190）**: 全 source が push 専用になったため、起動直後にまだ届いていない submit を待たず即終了する潜在バグが露呈し、この猶予窓で修正）／`--watch`（Orchestrator 側のポーリングはなく、push をイベントとして受け続け shutdown future で graceful 停止）／`dry_run`（**0.2.0**: push source はプレビュー対象を持たないため常に空を返す no-op、副作用ゼロ）。`settings_from_config` が `RootConfig` を解釈（`max_concurrency` F-40、`poll_interval_secs`（push source の内部 fetch 周期として `initialize` へ転送、F-06）、`[worktree] cleanup`/`plan_cleanup` F-23/85 — implement 既定 manual・plan 既定 immediate）。`[llm]` 未設定時は LLM が必要な選択を pending へフォールバック（F-14）。出力ポリシーは `source`=蓄積したエージェント出力を `result/publish` へ（F-07）と `none`=素通しの 2 つ。**`pull_request` は [ADR-0026](/decisions/adr-0026-agent-owned-branch-and-push.md) で廃止**し、`run::output` サブモジュール（`PrCreator` / `GhPrCreator` / `PrContext`）・`[output]` の PR テンプレート・`Engine::with_pr_creator`・`WorktreeManager::{push_branch, has_commits_to_publish}` ごと削除した（push と PR 作成はエージェントの責務。F-86 撤回）。enum ごと消したのは、受理して `source` 相当に降格させると **PR が作られなくなったことに気付かないまま運用される**ため。publish 失敗は worktree・コミット・セッションを保持したまま failed とし `task retry` で再開可能。あわせて `finalize_success` の冒頭で `sync_branch` を呼ぶ — **hook を持たないエージェントは Stop を送らない**ので、ここが無いと `record.branch` が未記録のまま出力ポリシーに入る。**#409: その直後に `read_only_side_effect` を検査**し、read-only profile（answer / triage / design）のタスクがブランチ上にあれば `fail_publish` で失敗させる（worktree とコミットは保持）。**止まるものは profile で違う**（実機 2026-08-11）: `answer` は返信をオーケストレータが publish するので**返信ごと止まる**が、`triage` / `design` はエージェントが `gh issue comment` で直接書く（#398）ため**成果物は既に出ており取り消せない** — 止まるのは成功報告と `on_success` だけである。**防止ではなく検出**で、ブランチがある時点で push は済んでいるかもしれず取り返せないが、「証拠を残して失敗した」は「完了と報告された」とは別の運用状態である（#410 が生んだのは後者）。門は `record.mode` ではなく **profile** で作る — 素の `mode = "plan"` を対象にすると既存構成がアップグレードで黙ってタスクを失う（[ADR-0036](/decisions/adr-0036-read-only-violation-fails-the-task.md) D3）。#136/#138 で `Engine::run` 開始時に `EngineSettings.hook`（`HookRuntime`＝socket_path＋解決済み Bearer token＋spool_dir＋ワークフロー別 `--settings` パス＋block_retry_limit、CLI が組み立て）があれば `hook_uds::bind`→`serve` を tokio::spawn（`[hooks]` 未設定でも既定パスで常時起動、bind 失敗は warn して継続）、ループ終了時に watch で停止・await。**#138: フックシグナル統合（`run::hooks`）**。`PluginEvent::HookSignal(AgentSignal)` は `on_event`→`Engine::on_signal` へ届き、job_id→task_id 解決（E-09。未知 task は warn ログのみで `hook_events` へ永続化しない＝`task_id` NOT NULL FK のため意図的・状態も一切変えない）→`touch_last_signal`（生存更新は冪等判定より前。重複 heartbeat でもアンカーを更新しタイムアウト誤判定を防ぐ）→冪等記録（`record_hook_event`、`Duplicate` は即 return D-05）→イベント別処理: `Stop{COMPLETED}` は `verification=llm/none` なら既存 Done 経路（マーカー除去済み `last_assistant_message` を `agent_output` へ→`finalize_success`／出力ポリシー無改修 R-07/R-11）・`human` なら `SelfReportComplete`→`Verifying`＋`VerificationPending` 通知＋成果物を永続化（再起動後の検収に備える D-01）、`NEEDS_INPUT`→`WaitingInput`（D-07）、`FAILED`→`Fail`（reason 保存）、`UNKNOWN`→遷移なし・連続 UNKNOWN が `block_retry_limit`（既定 3）以上で `Escalate`（D-02）、`Notification`→通知のみ（承認待ちと質問待ちの区別 R-08）、`SessionStart`→`set_tool_session_id`（相関確立、不一致は警告のみ・Escalate しない E-09）、`SessionEnd`→記録＋警告（Fail にはしない・タイムアウト/デッドマンに委譲 D-10）、`Heartbeat`→`touch_last_signal` のみ。`Escalate` 時は capability `diagnostics_snapshot` があれば `diagnostics/snapshot` RPC で pane スナップショットを取得し `events.detail` へ記録（R-10）＋`Escalated` 通知。`sweep_signal_timeouts()`（cycle ごと）がフック dispatch 済みの非待機タスクで `now - last_signal_at > workflow.timeout_secs`（既定 1800）を `Escalate`（D-03）。**`timeout_secs = 0` の workflow は掃引の対象外**（#439、[ADR-0042](/decisions/adr-0042-timeout-zero-opt-out.md) — attended pane 向けオプトアウト）。`replay_spool()`（recover 直後＋cycle ごと）が `[hooks].spool_dir` の `*.jsonl` を `hook_uds::parse_signal` で正規化し `on_signal` へ再投入（冪等キーで重複無害 E-07）。全行クリーンに処理できたファイルのみ削除し、parse 不能行を含むファイルは `<name>.corrupt` へ隔離リネームして保全（部分書き込み/壊れた1行で全体を失わない）。dispatch 側（`dispatch_one`）はフック対応 agent（判定は [plugin-protocol](/components/plugin-protocol.md) の `Capabilities::hook_completion`。**#496 以前は `resume_session \|\| diagnostics_snapshot` という de-facto の OR 判定**（`hook_capable()`）で、どちらのフラグも「hook」とは言っていないため作者は規約を知らないと opt-in できなかった。**#209 でこの OR 判定を単一ソース化**し、CLI 側の `[hooks].auth_token_ref` 検査と同一基準を共有する）に `job_id = job-{task_id}-{session_row}`＋`HookLaunchSpec { settings_path, env: TOTSUKA_JOB_ID/HOOK_ENDPOINT/HOOK_TOKEN/HOOK_SPOOL_DIR/PROMPT_CONTEXT }` を付与（session_row は起動前に `reserve_session` で確保し `task/dispatch` 後 `set_session_native_id` で埋める。dispatch 失敗時は `delete_session` で予約行をロールバックし空 id 行を残さない）。**#196: ツール解決と `tool_launch`** — repo/agent 検査直後に `wf.tool > repo.tool > default_tool` でツールを解決し（未知名・アダプタ無し kind は fail_dispatch）、`ToolProfile::launch_spec` が組み立てた完全解決済み argv/env を `TaskDispatchParams.tool_launch` として送出（deprecated の `hook` も移行窓の間は併送）。thread 継続 resume は `caps.resume && caps.session_id_capture` でゲート。**#196 Phase 3: ケイパビリティ駆動のコンテキスト経路** — `caps.invisible_injection` が無いツール（opencode）へのフック付き dispatch は、`TOTSUKA_PROMPT_CONTEXT` env の代わりに同内容（instructions + マーカー規約）を**可視の extra_context** へ載せる（完了契約は初回ターンから届く。pane に表示される点が claude/codex と異なる）。あわせて**ディスパッチのコンテキスト経路制御**: フック付き dispatch は `Task.instructions`（protocol 0.1.5、task_source 所有の指示文）とマーカー自己申告指示（`prompts` の `marker_self_report`、#313）を連結して **env `TOTSUKA_PROMPT_CONTEXT`** に載せ、CLI の `on-user-prompt-submit.sh`（`UserPromptSubmit` フック）が `additionalContext` として**不可視注入**する — ペインには本文だけが表示され、初回の指示にマーカー規約が含まれるため**最初の Stop からマーカーが載り、`on-stop.sh` の block による全文再生成（ペインの二重出力）が起きない**（実機検収での発見）。指示には**配信契約**も明記する（`slack-reply` 実機の 2 回目の発見）: ①公開されるのは**マーカー付き最終メッセージのみ**（`on_stop_completed` がその 1 通を成果物として stash する R-07/R-11）なので、最終メッセージは過去メッセージ参照を含まない自己完結の全文であること ②バックグラウンドタスク/サブエージェント実行中はマーカーを出さないこと（その Stop は R-02 ハートビートであり、完了後の再起動ターンで全文＋マーカーを再掲する）。この契約が無いと、回答済みのままバックグラウンド完了で再起動されたターンが「前のメッセージで提示済み」というメタ発言＋マーカーを出し、そのメタ発言が Slack へ公開されていた。このとき `extra_context` は `None`（可視前文は廃止）。マーカー知識は core に閉じ source プラグインは触れない（H-01）。マーカー行は publish 時に除去済み（R-07/R-11）で、`on-stop.sh` の block は付け忘れ時のセーフティネットとして残る。**フック非対応 agent（orca/mock）へのフォールバック**: 不可視チャネルがないため `Task.instructions` を可視の string `extra_context` として渡す（マーカー指示は付けない — フック完了報告を使わないため）。**#140: [会話継続](/glossary/conversation-continuity.md)（下記 #259/#264 で置き換え済み — 以下は履歴）**。`dispatch_one` はリトライ再利用ブロックの直前で resume 判定（`thread_resume_session_id`）: タスクが `thread_key` を持ち、同一 workflow・同一 thread_key の**先行**タスク（`find_by_thread_key(workflow, thread_key, exclude=自 id)` = 自分以外の最新、E-09）の最新セッションに空でない `tool_session_id` が確立し（フック SessionStart 由来 #138）、agent が `resume_session` capability を宣言していれば、`task/dispatch(resume_session_id)` に先行セッション ID を渡し `claude --resume` で文脈を継続する。worktree は通常フローで新規作成（破棄済みなら再作成 = セッションだけ使い回す D-10）。1 つでも欠ければ警告なしで通常の新規 dispatch へフォールバック（ベストエフォート）。新セッション行の `tool_session_id` に resume 依頼値を仮置きし SessionStart で実値と突き合わせる。先行タスクの行・状態は変更しない。`totsuka task verify --pass`→`ApproveVerification`（Verifying 以外はエラー）→次 recover で `finalize_success`、`--fail --reason`→`VerificationFailed`→Running（D-07）。**#155 (F-94): click-to-focus 制御**。`PluginEvent::Focus { task_id, respond }` を `on_event` が `run::hooks::Engine::focus_task` へ配路: task→`latest_session`→agent プラグイン解決、**`pane_control` 宣言時のみ** `session/focus` RPC（session_id は不透明のまま委譲 F-37）。task 不明/未 dispatch/プラグイン停止/`pane_control` 非宣言/pane 消失はすべて **reason 付き `FocusOutcome`（focused: false）の正常応答**でエラーにしない（[ADR-0005](/decisions/adr-0005-click-to-focus.md)）。**#185 (0.1.6, [ADR-0008](/decisions/adr-0008-task-submit-push-ingestion.md)): push 取り込み（`task/submit`）**。source プラグインごとの forwarder が `IncomingRequest` を消費し、parse（不正は `INVALID_PARAMS`）→ per-plugin Semaphore（枠 64、枯渇は `SUBMIT_OVERLOADED`）→ `PluginEvent::TaskSubmit { source, task, respond: oneshot }` を送出（channel closed → `NOT_ACCEPTING`、`task/submit` 以外は `METHOD_NOT_FOUND`）。ack 待ちだけ spawn し、イベント送出は inline のため per-source FIFO は維持。`Engine::on_task_submit` はプラグインが名指した `workflow` の解決（存在しない名前 / 別ソースの workflow → `Rejected` + reason。#554 でこれが `match_workflow` による再判定を置き換えた）の上で `ingest_task()` により **`upsert_task` コミット後にのみ ack**（persist-before-ack、重複は `Duplicate`）。応答後に `select_repos()` → ループ既存の `dispatch_ready()` で通常配車。永続化失敗は plugin へ retryable な `INTERNAL_ERROR` を返した上で run-fatal。`RunStats.submitted` 追加。**#260 (#242): `task/lookup` の応答**。forwarder を `forward_plugin_request` に一般化し（`IncomingRequest` は元々メソッド名非依存の汎用機構で、`task/submit` はその 1 実装）、`task/lookup` のアームを追加。`find_by_source(source, task_id)` を引いて `{known, repo?}` を返す**読み取り専用**の RPC で、in-flight budget は submit と**別枠**（片方のバーストが他方を枯渇させないため）。`source` は params ではなく**接続元**を使う（別ソースの会話を名乗って覗けないようにするため）。エンジンループで処理されるので `git fetch` 等で詰まっていると待たされるが、**読み取り専用なので失敗しても run-fatal にせず**応答して捨てる（プラグイン側の縮退＝従来どおりの解決が正しい挙動）。**#258 (#242、[ADR-0015](/decisions/adr-0015-conversation-task-identity.md)): ingest を会話への追記に対応**。`source_task_id` が会話キーになると `ON CONFLICT(source, source_task_id) DO NOTHING` は「同じスレッドの別メッセージ = 無視」を意味してしまい **2 通目が黙って捨てられる**（返信もされない）ため、**重複判定の唯一の点を `task_messages.message_key` へ移した**（`upsert_task_inner` の `DO NOTHING` 自体は不変で、その意味が「この会話は既にある」に戻るだけ）。`ingest_task` は `(id, IngestOutcome)` を返し、`Created`（会話新規）/ `Reopened`（終端 → `Reopen` で `Queued`）/ `Appended`（実行中。状態を変えず台帳で待つ）/ `Duplicate`（再配送。**何も変えない**）を区別する。ack は前 3 者が `Accepted`・再配送のみ `Duplicate`（ソースから見れば「受け取った」は会話の状態によらず同じ主張であるため）。`message_key` 未設定のソースは `Task.id` にフォールバックするので **GitHub / Notion は挙動不変**（1 回 ingest・再送は Duplicate）。会話の `title` は 1 通目のまま（スレッドの主題として扱う）。台帳は新規会話でも 1 行目を記録し、`author` は共通スキーマに著者フィールドが無いため NULL（`assignee` はソースごとに別人を指すので流用しない。全文は `payload` に残る）。`Reopen` の `detail` には `{"kind":"reopen", "message_key": ...}` を入れ、`events` から retry と reopen を区別できない件（#256）を補う。**#259 (#242): dispatch のメッセージ駆動化**。プロンプトは**未処理メッセージを時系列で連結**したもの（`conversation_prompt`）で、連投 3 件でも**1 dispatch = 1 返信**になり pane の二重起動も起きない。resume 済みセッションがそれ以前を保持しているので渡すのは新着分だけ。台帳が空なら record の body にフォールバック（#242 以前のタスク・v6 バックフィル行）。ディスパッチ**成功時のみ**`mark_messages_processed` でバッチ共通時刻を打つ（失敗時は retry のためキューに残す）。**resume の付け替え**: `thread_resume_session_id` / `find_by_thread_key` を削除し`latest_session(record.id)` の `tool_session_id` を使う（同一タスクなのでスレッド横断検索が不要になった。`reserve_session` が空行を作る前に読む必要がある）。**`SESSION_UNRESUMABLE` 再試行**: resume 付きで dispatch して当該コードが返ったら resume なしで**1 回だけ**再送する（再送はセッションを指名しないので同じ理由では落ちずループしない。文脈は失うがタスクは進む）。**`retry_task`**: `Retry` と `unprocess_last_batch` を同一トランザクションで行い、エージェント失敗後の retry が空プロンプトにならないようにする。**`requeue_conversations_with_unsent_messages`**: 実行中に届いたメッセージをingest は意図的に放置する（動作中のエージェントを割り込むと pane と返信が二重になる）ため、完了後に拾う掃除を `cycle()` の `dispatch_ready` 前に置く。`Cancelled` は対象外 — cancel 後に届いたメッセージは ingest が reopen する（新しい指示なので正しい）が、cancel 時点で既に台帳にあったものが人間の判断を覆してはならない。**`Failed` も対象外**（レビュー指摘）— dispatch 失敗はメッセージを未送信のまま残すため、requeue すると同じ恒久エラーで失敗して 200ms tick ごとに無限ループし通知も毎周回出る。戻すのは `task retry`（メッセージも一緒に戻る）という人間の操作に限る。走査は `conversations_with_unsent_messages` の 1 クエリ（tasks×task_messages の JOIN）で行い、毎 tick の N+1 クエリを避ける。また **retry-reuse 経路には `pending.is_empty()` ガードを置く**（レビュー指摘）— `recovery::retry_plan` は (worktree_path, branch, session) だけを見て状態を見ないため、#242 で再オープンした会話は**必ず reusable に見え**、re-attach が成功するとプロンプトを渡さず早期 return して**新着メッセージが無言で埋もれる**。`retry_task` も、未処理メッセージが既にある場合は `unprocess_last_batch` を行わない（dispatch 前に失敗した run はバッチを受け取っていないので取り戻すものが無く、1 つ前の回答済みバッチを復活させると二重実行・二重返信になる）。再起動リプレイは既存機構のまま（persist 済み未 dispatch の `Queued` 行を起動時 cycle が回収、ack≠dispatch）。**#190 (0.2.0, ADR-0008 Phase C): `tasks/fetch` 削除**。`TasksFetchParams`/`TasksFetchResult` ごと削除し、全 task_source が push 専用に。Orchestrator 側のポーリング機構一式（`fetch_and_ingest`/`poll_sources`/`poll_interval_for`/`IngestPath` enum/`EngineSettings.poll_intervals`）を削除、`ingest_task()` は無条件で `upsert_submitted_task` を呼ぶだけに単純化。`RunStats.fetched`/`.ingested` を削除（`fetched` は恒久的に 0・`ingested` は `submitted` と完全重複だったため）。この変更で「one-shot は起動直後の未着 submit を待たない」という潜在バグが露呈したため、`run()` に静穏期間の猶予窓（`ONE_SHOT_GRACE`）を追加して修正した（上記参照）。**さらに**（code review で発見）、旧ポーリング駆動の `cycle()` 呼び出しは実質的に周期メンテナンスの心拍も兼ねていたため、これを削除しただけでは `--watch` の長時間稼働プロセスが `sweep_signal_timeouts()`（D-03）・`sweep_finished_worktrees()`（F-23）を push イベント到着時以外に一切再実行しなくなる regression が生じていた。`SETTLE_TICK`（200ms）の安全 tick を one-shot 専用ガード（`if !watch`）から外し両モード共通の周期 `cycle()` 呼び出しへ変更して修正（`crates/orchestrator-core/tests/hook_integration.rs::watch_mode_periodic_tick_escalates_silent_task_without_events` で固定）。`^0.1` manifest は F-54 により起動拒否（意図された動作）。**#210 (0.2.1, [ADR-0010](/decisions/adr-0010-worktree-cleanup-pane-release.md)): worktree 掃除の3段化と pane 解放**。`cleanup_worktree` は `decide_cleanup`（判定）→ **`Remove` のときだけ** `release_pane`（`session/release` RPC。`pane_control` 宣言 agent のみ・`expect_cwd` = worktree パスを同一性ガードとして送出・`expect_label` は未送出）→ `remove`（削除、dirty 再チェック付き）の3段で実行し、pane の寿命を worktree の掃除ポリシーに連動させる。`Retain`/`Dirty` では pane を保持（F-23 の人間導線）。`Engine.released_panes` が release の一回性を保証（RPC が正常応答した時点で `released` の真偽によらず記録。削除が失敗し続けても再送しない。transport エラーは記録せず warn のみ = 次 sweep で再試行）。**キーは `sessions.id`**（#486 で `task_id` から変更） — 1 タスクは retry や追いメッセージで複数の pane を持ちうるので、task 単位だと「新しい pane ができたら手で無効化する」規約が要り、**書き忘れが無言で pane を漏らす**（#210 の症状）。セッション行は dispatch のたびに増えるのでキーが自動的に無効化され、#481 で `dispatch_one` に置いた手動リセット 2 箇所は消えた。参照は `finalize` の中だけに閉じている。**一回性を適用するかは呼び出し側が `ReleaseMode` で選ぶ**: 掃除は `Once`、再 dispatch は `Always` — 後者は新しい pane を開くための前提条件で、`released: false` が「既に消えていた」と「同一性拒否で閉じなかった」の両方を意味する以上、メモは pane が閉じた証拠にならない。この区別はテストより先に型が守る（`Always` の呼び出しを消すと `dead_code` でビルドが落ちる）。メモの刈り取りは `forget_release_memos` が `list_sessions` でタスクの全行を落とす（最新行だけでは、リトライしたタスクの古い行が残る）。release 失敗は削除をブロックしない（孤児 pane は #211 doctor の受け持ち）。あわせて **worktree sweep の間隔を `SETTLE_TICK` から分離**: `sweep_finished_worktrees` は `Retained`/`DirtySkipped` 1 件ごとに `git status` のプロセス起動を伴い、200ms tick のままでは `keep_7d`/`keep_28d` の長期保持で毎秒 5 回走り続けるため、`EngineSettings.worktree_sweep_interval`（既定 60s・config 非露出、テストは `Duration::ZERO` で毎 cycle）で間引く。起動直後の初回 cycle は必ず sweep（起動時回収は即時のまま）、done 直後の cleanup（`finalize_success` 直呼び）は間引き対象外で pane close は即時。`settings_from_config` は `[worktree]` の保持プリセット **`keep_7d`/`keep_28d`**（#210）を `RetentionDays(7)`/`RetentionDays(28)` へ desugar する（`CleanupPolicy` は不変・既定値も不変）。**#254: `dispatch_one` の worktree 再利用分岐にディレクトリ実在チェック**。記録済み `worktree_path` は「使える worktree がある」証拠にならない（`plan_cleanup = "immediate"` の完了時削除・手動 `git worktree remove`）ため `is_dir()` を条件に加え、無ければ**同一パスで再作成**する（パスは source + task_id と `location_template` から決まるため、設定が変わらない限り同じ値が再現する。ブランチは記録済みの名前を `existing_branch` として渡し、生存していれば載せ直す。Claude Code はセッションを worktree 外の作業ディレクトリ単位で保持するため resume は生存）。なお `is_dir()` は「使える worktree」の十分条件ではない（gitdir が切れた残骸は通る）。`WorktreeError::AlreadyExists` はワークスペース内のどこでも match されず生の git エラーに潰れていたので、専用アームで「このタスクの記録に無い worktree が当該パスを占有している」旨と対処（`git worktree remove` / doctor）を返す。**#481: dispatch は前回セッションの pane を先に閉じる**。`totsuka task cancel` は CLI プロセスで プラグインホストを持たないため DB を書くだけで pane を閉じられず、閉じるのは `cleanup_worktree` の `release_pane` — つまり `worktree_sweep_interval` の掃引待ちである。その窓の中で `task retry` すると 生きた pane の上へ dispatch することになり、task id から agent 名を導くプラグインは自分自身と衝突する（herdr は `agent_name_taken` を返し、dispatch は 1 秒未満で failed になり、後続の掃引でも回復しない）。同じ衝突は retry でなくても到達できる — worktree 削除より長生きした pane を持つ `done` タスクへの追いメッセージ（#242）。そこで `dispatch_one` は**一度でも dispatch されたタスク**（`latest_session` が有る）に対し、`wire_hooks` が 新しいセッション行を予約する**前**に `release_pane` を呼ぶ（予約後では `latest_session` が空の新行を返し、生きた pane へ届かない）。`task/cancel` ではなく `session/release` を使うのは `expect_cwd` の同一性ガードのため（herdr の pane id は位置ベースで、無防備な close は id が今指している別の pane を巻き込む）。冪等なので、掃引が先に閉じていた場合の追加コストは RPC 1 回だけである。当初はあわせて dispatch 成功時に `released_panes` から task を落としていたが、これは task 単位キーゆえの手当てで、**#486 でキーをセッション行 id に変えたため不要になり削除した** | #63 / #65 / #136 / #138 / #140 / #155 / #185 / #190 / #210 **#492: dispatch 失敗の自動再試行**。`fail_dispatch` は `Fail` を記録した後、`StateDb::auto_retry_streak` で「直近の成功 dispatch 以降に自動再試行した回数」を数え、`DISPATCH_RETRY_LIMIT`（定数 3）未満なら `retry_task` で再キューして**通知も書き戻しもしない**。使い切ったときだけ従来どおり `Failed` + `NotifierEvent::Failed`。動機は実機の `agent_prompt_stalled`（pane と CLI は起動したがプロンプトが submit されず 42 秒で失敗、同じタスクを人手で retry したら無改変で通った）で、**人間の修復手順が `task retry` 一手だけ**だったため機械化した。**上限が設計の中心**である — `requeue_conversations_with_unsent_messages` が `Failed` を除外している理由（上限なしで requeue すると毎 tick 同じ失敗を繰り返し通知も毎回出る）はそのまま生きており、回数を数えることがその唯一の違いになる。逆に**何が失敗したかは一切見ない**: 上限がある以上、恒久的な失敗に 3 回使う無駄より、分類を誤って「もう一度で直る失敗」を切り捨てる害のほうが大きい。回数はプロセス内ではなく `events` から毎回数え直すので `run` の再起動をまたいで保たれ、人間の `task retry`（`detail.kind = "cli"`）が走査を止めるため**予算が自動でリセットされる** / #254 / #481 / #486 |
| `adapters` | ports の具象実装。`state_db`（SQLite 状態永続化・埋め込みマイグレーション・イベントログ・**タスクノート（#407。`note_task` / `open_notes` / `NOTE_KEY`。`from_state == to_state` の非遷移行を `events` に書き、「そのタスクの最新 event か」で未解消を判定する → [ADR-0037](/decisions/adr-0037-task-notes-in-the-event-log.md)）**・冪等取り込み・セッション永続化（`record_session`/`latest_session`/`list_sessions` F-37）→ [state.db スキーマ](/data/state-db.md)）、`run_lock`（多重起動防止 F-74）、`plugin_host`（プラグインをサブプロセス起動し NDJSON JSON-RPC で通信、initialize[task_source には PluginSpec.repositories = config.toml の [[repositories]] を供給 #109、PluginSpec.llm = config.toml の [llm]（api_key_ref 解決済み）を分類用 default として供給 #119]/protocol 互換チェック/リクエスト相関/タイムアウト/クラッシュ隔離/config·validate 委譲、F-51/54/58/59/65, §5.3。**#497: `Inner::call_raw` が全 O→P 呼び出しの単一の関門**なので、そこ 1 箇所に `plugin_rpc` span と `CallOutcome`（`ok`/`timeout`/`crashed`/`closed`/`rpc_error`/`json`/`io`）別の会計を置いた。ok/error の 2 値に潰さないのは運用者が取る行動が違うため（`timeout` は生きているが遅い、`crashed` はもう居ない、`rpc_error` は答えたうえで拒否した）。stderr 転送は 10 秒あたり 100 行にレート制限し、超過は「N 行抑制」に畳む（**redaction はしない** — ホストはプラグインが何を秘密と考えているか知らないので正しく消せない）。**#495: `Liveness`（`Live`/`Crashed`/`ShutDown`）を `watch` で配る** — 「死んだ・理由はこれ」までを報告し、どうするかは決めない（判断は `run::supervise`）。`Crashed` と `ShutDown` の区別が要るのは、トランスポートから見ると両者が同じ（子が消え stdout が閉じる）ためで、区別しないと正常終了のたびに全プラグインを再起動する。マーキングは**先着優先**で、`shutdown()` が子を消しうる操作の前に `ShutDown` を書くため後続の EOF に上書きされない。**#184（0.1.6）: プラグイン起点 request（P→O、`task/submit` 等）の受信** — reader が `method`+`id` を持つ行を `IncomingRequest { method, params, responder }` として `take_incoming_requests()` の channel へ配路（Notification 判定より**前**。従来はプラグイン起点 request が Notification として黙って破棄され id が失われていた）。応答は `Responder::ok/err` が共有 writer channel へ Response 行を書き戻すため O→P リクエスト行と交錯しない。プラグイン exit で channel close、孤児 Responder への応答は無害な no-op）、`agent_session`（`PluginAgentSession`＝`AgentSession` の実装、`session/attach`＋`state/subscribe` 再確立 F-37）、`hook_uds`（#136。UDS 受信サーバ driving adapter。`bind`＝stale ソケット unlink→`UnixListener` bind→0600 設定、`serve`＝接続ごと tokio::spawn・最小 HTTP/1.1 パーサ（`\r\n\r\n` までヘッダ→`Content-Length` body、chunked/method 非検査、path は**完全一致 `/focus` のみ**制御エンドポイントへルーティングし他は全てシグナル受信 E-08）・Bearer 定数時間検証 E-03・body>1MiB は 413・JSON→`AgentSignal` 正規化（未知フィールドは payload に温存、job_id 欠落/不正は 400 E-09）・`SignalPort::submit` 後即 200 E-04・**`/focus`（#155 F-94）は `{"task_id"}` を `FocusPort::focus` へ渡し `FocusOutcome` を JSON 応答**（[POST /focus](/apis/agent-events.md)）・watch シグナルで graceful 停止しソケット unlink）、`engine_signal_sink`（#136/#155。`EngineSignalSink`＝`SignalPort` + `FocusPort` 実装、`PluginEvent::HookSignal` を Engine のイベントチャネルへ転送する薄いラッパ（状態を持たず重複投入は素通し＝冪等は DB 層の責務 D-05）。focus は `PluginEvent::Focus { task_id, respond: oneshot }` を送り応答を await する request-response）、`clock`（#174。`SystemClock`＝本番実装（`OffsetDateTime::now_utc()`）と `ManualClock`＝テスト用の手動駆動時計（`Mutex<OffsetDateTime>` + `set`/`advance`。integration test から使うため `#[cfg(test)]` ではなく public — `test-support` 配置は arch-lint の dev エッジ込み循環検査に抵触するため不採用）。注入は `Arc<dyn Clock>` フィールドで、`StateDb::open_with_clock` / `open_in_memory_with_clock` / `Engine::with_clock` が注入口。既存の `open`/`new` は `SystemClock` を既定装着しシグネチャ不変。DB と Engine には**同一の Arc** を渡す規約（別々だとタイムラインが分裂する）） | #48 / #51 / #57 / #136 / #155 / #174 |

## 公開型（#46 時点）

- `paths::Paths` — `from_system()` / `from_env(fn)`（テスト用に環境注入可）、各 `*_dir()` アクセサ。
- `ports::SecretString` — `Debug`/`Display` で値を露出しない newtype（§5.2 のログ流出防止）。`expose()` で明示的に取り出す。
- `ports::SecretRef` — 秘密参照のパース済み表現（`keychain:<service>/<account>` / `op://<vault>/<item>/<field>` / `cmd:<command>`、#444）。
- `ports::SecretStore` / `ports::ProcessProbe` — OS 依存機能の trait 境界。
- `ports::Clock` / `adapters::clock::{SystemClock, ManualClock}`（#174） — 壁時計の trait 境界と実装。`Arc<dyn Clock>` で `StateDb`・`Engine` へ注入（既定 `SystemClock`、テストは `ManualClock` を `open_with_clock` / `open_in_memory_with_clock` / `Engine::with_clock` から注入）。

# 依存

- `thiserror`（エラー型）。`libc`（unix、プロセス生存確認）。`keyring`（macOS 限定、Keychain）。
- 参照元: [orchestrator-cli](/components/orchestrator-cli.md)。

# 関連

- [ADR-0002 Rust workspace 構成と CI 品質ゲート](/decisions/adr-0002-rust-workspace-ci.md)
- [ADR-0004 フック完了シグナルの受信配置（ports::SignalPort + adapters::hook_uds）](/decisions/adr-0004-hook-completion-signal.md)
- [フックシグナルフロー](/architecture/hook-signal-flow.md) / [フックのセキュリティ](/security/hook-security.md) / [フックのトラブルシューティング](/operations/hook-troubleshooting.md)
- [POST /agent-events（UDS フック受信）](/apis/agent-events.md) / [state.db スキーマ](/data/state-db.md)
- [Spec §6 技術要件 / F-100〜F-107](/product/orchestrator-spec.ja.md)
